# 更新日志

## [1.11.0] - 2026-09-08

### 变更（修正 1.10.0 的实现方式）
- **早报正文保持原样**，周榜改为**两张独立图片**追加在后面：
  1. 先发原来的早报文字（有比赛时才有，内容与排版完全不变）；
  2. 再发「本群本周进步榜」图片；
  3. 最后发「本群本周退步榜」图片。
  图片与交互指令 `本周进步榜` / `本周退步榜` 完全一致（四平台分栏、每平台前 5 名）。
- 1.10.0 曾把周榜**文字**合并进早报正文，导致早报被拉长并可能被整段转图；
  该实现已被本次替换。
- **当天没有比赛时**：不发早报正文，但仍会推送这两张榜单图片；
  两张都没有数据时才算无内容（不推送）。
- 图片渲染不可用时，自动回退为该榜单的纯文本（`_weekly_board_text`），信息不丢。
- WebUI「测试推送」同样会追加这两张图，便于后台预览。

### 实现说明
- 新增 `build_weekly_board_cards()`（渲染两卡，复用 progress 快照，零额外抓取）、
  `push_weekly_boards()`（按 进步榜→退步榜 顺序发送图片，失败回退文字）、
  `_send_group_image()`（主动发送图片，复用现有会话就绪检查）。
- 调度顺序：正文 → 图片；正文已送达即标记完成（避免重复早报），
  仅周榜失败时记录日志；无正文时若周榜失败则不标记，下个周期重试。

### 测试
- 重写 `tests/test_morning_boards.py`（9 条）：两卡生成与顺序、
  退步榜只含下降成员、无数据返回空、单平台异常不影响另一平台；
  调度层覆盖「正文原样 + 随后推图」「**无比赛仍推图**」「图表皆空则标记完成」
  「无正文时图失败则重试」「正文成功但图失败仍标记完成」；全量 **158 passed**。
- 生产真实数据渲染核对：进步榜卡 1200×1391、退步榜卡 1200×752，均正常生成。

## [1.10.0] - 2026-09-08

### 新增
- **每日早报附带周榜**：早报推送里自动追加「📈 本周进步榜」与「📉 本周退步榜」，
  每个平台显示前 3 名（`昵称 +变化（当前 Rating）`），并附一句完整榜单指令提示。
- **当天没有比赛时不再跳过早报**：此前无比赛会直接标记“已处理”并跳过，
  现在改为 —— 早报正文省略，但**只要周榜有数据就照常推送**；
  两者都为空时才跳过。
- 作为早报的一部分，**沿用早报的开关与推送时间**（`morning_push_time`），
  不新增配置项。
- WebUI 的「测试推送」也会包含周榜，便于直接预览早报完整形态。

### 实现说明
- 周榜与交互指令共用同一份 progress 快照（`rank_service.read(progress=True)`），
  **不额外抓取 OJ 数据**；任一平台失败只跳过该平台并记录日志，不影响比赛早报。
- 新增 `build_weekly_boards_text(group)`；调度改为「早报正文 + 周榜」合并发送，
  日志会区分「今日无比赛，仅推送周榜」与「今日无比赛且暂无周榜数据，跳过早报」。
- 无周榜数据（没有成员有完整一周快照）时返回空串，不会产生空白区块。

### 测试
- 新增 `tests/test_morning_boards.py`（8 条）：周榜文本含双榜且排序正确、
  仅取前 N、无数据返回空、单平台异常不影响另一平台；
  调度层覆盖「有比赛+周榜」「**无比赛仍推周榜**」「两者皆空则跳过并标记」
  「只有比赛时只发早报」；全量 **158 passed**。
- 生产真实数据核对：周榜文本 259 字 / 15 行，内容形如
  `1. kjhhjki +101（当前 2552）`、`1. 不尽虹蛟朝天飞 -59（当前 2381）`。

## [1.9.1] - 2026-09-08

### 修复
- **排行失败提示过于含糊**：此前只输出
  `⚠️ 部分账号同步失败，排行可能不完整：洛谷、AtCoder`，
  既不说失败了多少账号、也不说原因和影响，用户会误以为插件坏了。
  现在改为精确提示，例如：
  `⚠️ 本次有账号资料同步失败：洛谷 8 个账号（洛谷：资料暂时无法读取）、
  AtCoder 8 个账号。常见原因是平台临时限流或账号资料暂不可读，稍后重试即可；
  这些账号本次不计入排行。`
  包含：平台、失败账号数、一条原因（超长截断到 40 字）、影响范围。
- 该汇总逻辑抽成 `_rank_error_notice()`，便于测试与复用。

### 排查结论（本次报告的“什么鬼”）
- 生产日志显示 10:39 洛谷 8/8、AtCoder 8/8 全部失败（洛谷 0.01s 命中负缓存，
  AtCoder ~10s 超时），而同期 CF/牛客正常；
- 事后复测：两平台 **8/8 全部成功**（8 并发耗时 2.72s / 1.21s），
  负缓存表也已清空 —— 属于**平台侧瞬时失败**（限流/超时），并非插件缺陷；
- 已确认与并发度无关（2 并发与 8 并发均成功）。

### 测试
- 新增 4 条断言：平台与数量、原因包含、按失败数排序、空输入返回 None、
  超长原因截断；全量 **150 passed**。

## [1.9.0] - 2026-09-08

### 新增
- **本周退步榜**：新增 `本周退步榜`（别名 `本周掉分榜`、`退步榜`），
  展示各平台近 7 日 Rating **下降最多**的成员（下降幅度从大到小）。
  排版与「本周进步榜」完全一致：四平台分栏、每平台前 5 名、
  `近7日变化` 为主指标、右侧附 `当前 Rating`。
- 设计上**与进步榜共用同一份数据**：两者都基于 progress 快照里的
  `近 7 日变化`，区别只在筛选与排序 ——
  进步榜取 `delta > 0` 降序，退步榜取 `delta < 0` 升序（例如 -120 排在 -30 前）。
  因此**零新增网络请求、零新增存储表**，快照新鲜窗口同为 2 小时。
- 无成员下降时给出明确文案：`🎉 本周没有成员 Rating 下降（也可能是成员暂无完整一周快照）`，
  而不是复用「当前群还没有加入排行的成员」。
- 已加入菜单文案。

### 说明
- 只统计有「完整一周快照」的成员（与进步榜口径一致），
  没有历史基线导致 `delta` 为空的成员不计入。

### 测试与验证
- 新增 `tests/test_regress_rank.py`（6 条）：指令别名与页码解析、
  只保留下降成员并按下滑幅度排序、脏数据/空输入容错、不修改原始行、
  并列时按昵称排序；全量 **146 passed**。
- 用**生产真实数据**端到端渲染核对：CF 下降 3 人（-59/-53/-6）、
  AtCoder 1 人（-41）；录制绘制坐标确认标题为「本群本周退步榜」、
  主指标为「近7日变化」、**越界文本 0、同行重叠 0**（并以进步榜作对照）。

## [1.8.2] - 2026-09-08

### 修复
- **热力图内部间距过紧**（用户实测 `查询atc StarSilk 详细` 反馈「标题/末行文字挤在一起」）：
  实测坐标显示标题与方格之间只有 **4~5px**、方格与图例之间只有 **8px**，
  在小尺寸查看时看起来像重叠。现在改为：
  - 标题与方格间距 → **11px**；
  - 方格与图例间距 → **12px**；
  - 区块顶部/底部留白 → 6px（原来 4px）。
- **间距改为共享常量**：新增 `ACTIVITY_PAD_TOP / ACTIVITY_TITLE_LINE /
  ACTIVITY_LEGEND_GAP / ACTIVITY_LEGEND_LINE / ACTIVITY_PAD_BOTTOM`，
  绘制与高度估算共用同一套公式，避免两者再次漂移（本次修了两处估算与
  实际绘制相差 4px 与 12px 的问题）。
- HTML 路径同步加大留白（标题下边距 8→12px、图例上边距 8→12px）。

### 验证
- 同一案例（AtCoder `StarSilk`）精确坐标复测：上方文字→热力图 38px、
  标题→方格 11px、方格→图例 12px、热力图→下方 Rating 趋势 18px，
  与其它绘制重叠数 **0**；
- 全量 **139 passed**。

## [1.8.1] - 2026-09-08

### 修复 / 优化
- **热力图空态不再“整块消失”**：此前近 12 个月没有提交时（例如长期未做题、
  提交都在一年前的账号），`activity_daily` 为空会导致热力图区块完全不渲染，
  用户会误以为功能没生效。现在改为显示一行说明：
  `近 12 个月暂无提交记录（历史共 82 条提交，均在一年前）`，
  其中历史提交数取自分析结果，帮助理解“有提交但不在近一年”。
- 严格区分两种“没图”的情况：
  - **平台不支持**（牛客/洛谷，分析里没有 `activity_daily` 字段）→ 不显示该区块；
  - **平台支持但近一年无提交** → 显示上述说明文字。
- 对齐热力图高度估算与实际绘制公式（此前相差 4px），避免卡片高度预算偏差。
- 文档补充指令口径说明：`我的cf`/`@查询` 总是抓分析（有热力图）；
  `查询cf <账号>` 默认只抓基础资料（需加「详细」才有热力图与难度分布）。

### 测试
- 新增空态与“平台不支持”两组断言（说明文案含历史提交数、HTML 走空态分支、
  牛客不出现区块），并修正高度一致性断言；全量 **139 passed**。

## [1.8.0] - 2026-09-08

### 新增
- **战绩卡「近 12 个月打卡」热力图**（Codeforces / AtCoder）：
  7 行（周一~周日）× 最多 53 列的方格矩阵，按当天**提交条数**分 5 档着色
  （0 / 1-2 / 3-5 / 6-10 / 10+），并显示「一年活跃 N 天 · 当前连续 X 天 ·
  最长 Y 天」。位置在「难度分布」与「Rating 趋势」之间。
- 双平台卡宽度只有一半，热力图自动降级为 **26 周**（近 6 个月）紧凑版；
  极窄时退化为纯文字摘要，绝不挤压。
- 数据来源：Codeforces `user.status`、AtCoder kenkoooo 提交记录，
  **复用已有的提交扫描结果，零新增网络请求**；随分析缓存 12 小时。

### 优化
- 提交扫描翻页只以「读完公开记录 / 到达上限」为准，不再为热力图提前收手
  （提前收手会让重度账号的提交次数/通过题数重新被截断到 1 万）。
- `ANALYSIS_CACHE_VERSION` 提升到 3：分析口径变化后旧持久化缓存自动失效，
  卡片立即显示热力图，无需等待 TTL。

### 测试
- 新增 `tests/test_activity_heatmap.py`（12 条）：按天聚合、北京时间分日、
  一年窗口裁剪、未来时间戳忽略、当前/最长连续（今天未提交不归零）、
  档位阈值、列矩阵结构与未来占位、Pillow 绘制格子数与边界、
  HTML 单元格与图例、无数据时不绘制；全量 **138 passed**。

### 渲染核对（发布前置）
- 真实账号渲染：CF `maspy`（21459 提交）/ `ygy0`（少量）、AtCoder `tourist`，
  以及双平台卡；
- 录制全部绘制调用并核对坐标：**热力图与其它绘制重叠数 = 0**、
  越出画布文本 = 0、卡片底部余量 389~579px；
- 裁剪热力图局部放大目视确认：行列对齐、无缺角/裁切、图例与统计文字完整
  不重叠、5 档配色正常；
- 整卡目视确认：热力图位于难度分布与 Rating 趋势之间，间距正常，
  底部页脚完整未被裁切。

## [1.7.1] - 2026-09-08

### 修复
- **旧分析缓存导致「数据分析摘要还是 1 万」**：提高了扫描上限后，个人资料卡的
  分析结果仍被缓存 12 小时（持久化在 SQLite `profile_cache`，重启也不会清），
  因此继续显示旧的 `最近 10000 条公开提交（最多读取 10000 条）`。
  现在引入 **分析缓存版本号 `ANALYSIS_CACHE_VERSION`**：
  - 分析类缓存的持久化 kind 带版本后缀（`analysis_v2` / `analysis_s_v2`）；
  - 加载时只接受当前版本，旧版本（`analysis` / `analysis_s`）直接跳过，
    下次查询即按新上限重新抓取，无需等待 TTL 或手动清库；
  - 以后调整扫描上限或分析口径，只需把该版本号 +1。
- 生产库实测：11 行缓存中 10 条旧口径分析缓存已被自动忽略，仅保留 1 条 basic。

### 测试
- 新增分析缓存版本化测试（新版本 kind 可还原、旧版本 kind 被丢弃、
  basic/detail 不受影响、kind 与 key 双向一致）；全量 128 passed。

## [1.7.0] - 2026-09-08

### 优化
- **提高提交扫描上限**：Codeforces `10000 → 50000`，AtCoder `10000 → 20000`。
  此前重度选手（LGM 等）的提交数被硬截断在 1 万，导致「提交次数 / 通过题数 /
  难度分布」明显低估。实测 `maspy` 共 **21459** 条提交，旧上限下统计被砍掉一半以上。
- **Codeforces 改为翻页扫描**：CF 单次请求文档上限为 1 万条（且插件请求超时为
  10 秒，超大 count 会超时），因此按 `CF_SUBMISSION_PAGE_SIZE=10000` 翻页，
  直到某页不满（读完公开记录）或到达 5 万上限。
  - 普通账号第一页就不满 → **仍然只发 1 次请求**，开销与旧实现相同；
  - 实测 2 万+ 提交的账号需 3 页、约 11.5s（结果缓存 12 小时，
    且只在个人资料卡的分析路径触发，群排行/进步榜不受影响）。
- **修复覆盖率文案自相矛盾**：旧逻辑用 `len(rows) < 10000` 判断是否读完，
  导致出现「最近 21459 条公开提交（最多读取 10000 条）」这种矛盾提示；
  现在按「最后一页是否满页」判断，读完显示「已读完当前公开记录」。
- AtCoder 分页增加 0.3s 页间间隔（2 万条最多 40 页），避免把 kenkoooo
  公共接口打得太急。

### 测试
- 新增 `tests/test_submission_scan.py`：上限值断言、小账号单次请求、
  重度账号翻页到短页、到达上限标记未读完、覆盖率文案一致性；全量 126 passed。
- 端到端实测：`maspy` → 3 次请求、提交 21459、通过题数 12830、文案正确；
  AtCoder `tourist` → 4 页 1630 条、9.8s。

## [1.6.2] - 2026-09-08

### 修复
- **段位缩写修正**：`legendary grandmaster` 由 `LG` 改为社区标准的
  **`LGM`**（此前少了一个 M）。
- **消除「Rating 的值是段位」的误读**：头部原来把「当前段位」直接跟在
  `Rating` 标签后面（`2947  Rating  LGM`），容易被理解为 Rating 的取值是
  段位。现在统一显示为 **`当前段位：LGM`**，与左侧大号 Rating 数字明确区分。
- **HTML 路径同步缩写**：此前只有 Pillow 路径做缩写，HTML 截图路径仍显示
  `legendary grandmaster` 全文，两条渲染路径不一致；现在共用
  `_rank_display_text()`，显示完全一致。

### 说明
- 卡片上「当前 Rating 数字」一直在（大号字体），本次只是把旁边的段位文案
  标清楚；`最高 Rating` / `最高段位` 分别对应历史最高值与历史最高段位。

### 测试
- 新增 `_rank_display_text()` 断言（CF 段位带前缀并可缩写、AtCoder 用 color、
  无数据回退「未评级」），LGM 缩写回归断言；全量 121 passed。

## [1.6.1] - 2026-09-08

### 修复
- **修复战绩卡「最高段位」与相邻文字重叠**：Pillow 卡片路径此前所有文本都用
  固定列宽绘制，完全没有测量/截断，`最高段位：International Grandmaster`
  这类长文本会越界压住右侧单元格（单平台卡列宽仅 252px，该文本约 323px）。
  现在按「缩写 + 自适应列数 + 省略号兜底」三层处理：
  1. **段位缩写**：Codeforces 标准段位显示为社区通用简称
     （`international grandmaster → IGM`、`legendary grandmaster → LG`、
     `candidate master → CM` 等），最高段位与头部当前段位共用同一套缩写；
  2. **自适应列数**：统计网格先测量最宽单元格，再决定 4/3/2 列，
     例如自定义头衔过长时自动从 4 列降到 2 列，信息不再被挤压；
  3. **省略号兜底**：仍超宽的单元格用 `…` 截断，保证永不越界。
- **修复头部「当前段位」可能越出卡片**：双平台卡该位置可用宽度仅约 123px，
  现在按剩余宽度截断。
- 说明：Codeforces 的 `maxRank` 可能是**用户自定义头衔**（实测有用户直接把
  用户名当作称号），长度不受控，因此不能只靠缩写，必须有测量型布局兜底。

### 测试
- 新增 `tests/test_account_card_layout.py`：段位缩写映射、自适应列数
  （含 2 列下限与空数据）、单元格右边界不越界、头部截断宽度；
  全量 120 passed。
- 另用真实字体度量做了三组渲染探针验证（单平台长段位 / 自定义超长头衔 /
  双平台卡），逐个单元格核对右边界均留有间距。

## [1.6.0] - 2026-09-08

### 新增
- **未绑定用户战绩查询**：新增 `查询cf <用户名>`、`查询atcoder <用户名>`、
  `查询牛客 <数字UID>`、`查询洛谷 <数字UID>`，输出与 `@查询战绩` 一致的资料卡
  （渲染失败自动回退纯文本）；同时兼容 `查询 cf <账号>`（带空格）与 `查cf` 简写，
  也接受主页链接。
- 结尾加「详细」才会抓取难度分布/分析（如 `查询cf jiangly 详细`），
  默认只出摘要卡，查询更快。
- 群聊结果附带绑定引导（私聊不加）。
- 防滥用：每用户 30 秒 1 次 + 每群每分钟 10 次，超限提示剩余等待时间。

### 说明
- 未绑定查询**不写入 Rating 快照、不参与群排行/进步榜**，避免污染排行数据；
  卡片不展示「本群排名」，标题标注「（未绑定）」。
- 平台能力限制：**只有 Codeforces / AtCoder 支持用户名查询**；
  牛客、洛谷公开接口只能按数字 UID 或主页链接查询（洛谷用户名搜索接口实测
  需登录态，公开不可用），命中非法标识时会明确提示改用 UID。
- 新增 `docs/未绑定用户战绩查询方案.md` 设计文档。

### 测试
- 新增 `tests/test_account_lookup.py`：指令别名/空格、详细后缀、
  不误伤既有指令（`查询战绩`/绑定/精确指令）、平台标识归一化边界、
  限流判定（冷却 + 群窗口）；全量 115 passed。

## [1.5.1] - 2026-09-08

### 修复
- **修复菜单退化为文字版**：1.3.2 为保留短文本换行，把 `get_settings()` 移到
  `needs_image()` 判断之后，导致首次请求时渲染阈值仍是渲染器默认值
  （`max_lines=36`），32 行的菜单被判为“不需要转图”而发文字。
  现在改为在热路径上**同步应用缓存的阈值配置**（不 await，仍不丢换行），
  并在插件启动时预读一次配置，保证 `needs_image` 始终使用管理员配置值。
- **进步榜计算大幅提速**：进步榜改用轻量资料（Codeforces 走批量 `user.info`），
  差值优先由本地 Rating 历史计算；仅对本地缺基线的成员限量回退拉取详细资料
  （每轮 8 人并轮转），后台刷新则允许完整补齐。
  此前首次计算需逐个成员拉取 Rating 历史，Codeforces 22 人受 2.1 秒限速
  影响实测耗时 **93 秒**。

## [1.5.0] - 2026-09-08

### 优化
- **本周进步榜纳入快照**：新增 `progress_snapshot` / `progress_meta` 表，
  与群排行共用同一套 stale-while-revalidate 读模型；此前该指令每次请求都会
  同步全群重算，现在命中快照即 O(1) 返回，过期时先回旧数据并后台刷新。
- 进步榜新鲜窗口为 **2 小时**（7 天聚合指标变化更慢），排行仍为 60 分钟；
  绑定/解绑/改名会把两个快照一起标脏。
- 后台 `refresh_stale` 分别扫描两种快照；进步榜只刷新"曾被查询过"的群，
  不会为全量群做昂贵的重算。
- 存储层快照读写方法统一支持 `mode="rank"/"progress"`，两种快照表相互隔离。

### 测试
- 新增进步榜落库/命中快照/独立标脏、rank 与 progress 表隔离的回归测试
  （109 passed）。

## [1.4.0] - 2026-09-08

### 优化
- **比赛数据后台预热**：新增 `ContestFetcher.warm()`，在缓存 TTL 到期前
  90 秒由定时任务提前刷新（每 tick 最多 1 个平台），用户请求不再撞上
  “缓存过期后同步抓取”的卡顿；预热失败按平台指数退避（5 分钟起，最长
  30 分钟），避免失败风暴打爆对端。
- **修复 force 语义**：`fetch_platform(force=True)` 此前仍会命中未过期缓存，
  现在会真正绕过旧缓存（保留并发去重），`更新` 指令与预热行为符合预期。
- **资料新鲜窗口对齐评审结论**：账号摘要/详细资料缓存由 10 分钟放宽到
  30 分钟，减少同一账号资料的重复抓取；`刷新我的战绩` 等 force 路径仍即时。
- **渲染性能与稳定性**：
  - 渲染器与字体探测结果增加进程级缓存（10 分钟），不再每次渲染都跑
    `which`/`fc-match`；
  - 渲染失败增加 300 秒冷却，坏渲染环境下直接走纯文本兜底，不再反复等超时；
  - 渲染产物增加 PNG 魔数校验，损坏/截断图片不再被当作成功发送，并会删除重绘。
- **缓存容量控制**：`output_cache/`、`account_cards/`（含头像）按总容量
  500MB 做 LRU 清理，每 5 分钟检查一次，避免磁盘无限增长。

### 测试
- 新增比赛预热命中/跳过、预热失败退避等回归测试（107 passed）。

## [1.3.2] - 2026-09-08

### 修复
- 修复短文本被动回复丢失换行的问题：`_adaptive_results` 在 yield 纯文本前
  不再调用 `await get_settings()`，避免 AstrBot 被动回复链路把 Plain 内的
  换行压平；长文本转图逻辑保持不变。

## [1.3.1] - 2026-09-07

### 优化
- 纯文本分片保留前导缩进，并优先在行尾/空格边界断片，避免丢缩进或硬切长链接。
- 超过单条安全长度但配置未要求转图的长文本改为按安全长度分片发送。
- 主动推送的文字分片单片失败时继续尝试剩余分片，并记录精确失败位置。

## [1.3.0] - 2026-09-07

### 优化
- 账号、群成员、待确认绑定与 Rating 快照从 AstrBot KV 迁入本地 SQLite
  （WAL），首次启动自动迁移并生成 JSON 备份，支持 KV 双写与回退。
- 资料缓存与账号级负缓存持久化到 SQLite，重启后避免全量冷启动重爬；
  按周期批量落盘并清理过期行。
- 群排行改为物化快照 + 后台刷新（stale-while-revalidate，默认 60 分钟），
  普通查询优先命中本地快照，不再全群同步抓取。
- settings/groups 增加内存缓存；定时任务的 reminded 与平台比赛数据
  每个 tick 只读取/抓取一次。
- Codeforces 批量 user.info 失败时仅回退失败批次，并用二分定位失效账号；
  增加账号级负缓存，避免坏账号反复拖慢排行。
- 排行缓存 key 去除 record_metrics，自己查询与被 @ 查询共享同一次计算。
- 增加渲染并发上限、账号抓取缓存上限、关键路径耗时日志。

### 兼容
- 默认仍保持用户可见指令/文案不变；`刷新我的战绩` 等 force 路径即时抓取。
- 环境变量：`ACMER_STORE_BACKEND`（sqlite/kv）、`ACMER_STORE_DIR`、
  `ACMER_DUAL_WRITE_KV`；如需回退旧 KV 模式可设置 `ACMER_STORE_BACKEND=kv`。

## [1.2.28] - 2026-09-06

### 修复
- 菜单、比赛长消息和其他长文本中的 Emoji 不再直接交给中文字体绘制，
  避免服务器缺少 Emoji 字形时出现“方框里的 X”。
- 插件内置 Noto Color Emoji 字体；HTML 渲染和 Pillow 回退均可使用，
  不要求服务器安装字体、安装系统软件或提供 Google Chrome。
- Pillow 无法按小字号直接加载彩色 Emoji 时，会先以高分辨率绘制再缩放，
  并让换行测量与实际混合绘制保持一致。
- Chromium 渲染允许加载 HTML 同目录的内置字体文件，图片缓存格式升级，
  旧版图片会自动重绘。

## [1.2.27] - 2026-09-06

### 修复
- 修复本周进步榜洛谷平台排名列标题过长被省略的问题；
  当前值列改用更紧凑的“排名”表头，保留指标含义并适配窄列显示。

## [1.2.26] - 2026-09-06

### 优化
- 取消单独 `@某人` 即触发战绩卡的行为，必须追加“战绩”“查询战绩”等明确后缀，
  降低群聊普通 @ 消息误触发的概率。

## [1.2.25] - 2026-09-06

### 新增
- 群聊支持直接 `@某人`、`@某人 战绩`、`@某人 查询战绩` 查询该成员的竞赛战绩卡，
  复用现有账号资料、Rating 变化和群排行展示。
- @ 查询为只读操作，不会更新被查询用户昵称，也不会自动加入或修改对方的群排行状态；
  多人 @ 或带未知后缀时不会误触发。

## [1.2.24] - 2026-09-05

### 优化
- 重新调整资料卡和排行卡的文字配色：浅色卡片使用更深的紫褐色正文，
  深色背景使用更亮的标题、备注和页脚文字。
- 正负变化统一使用更清晰的深绿色/深红色，平台标题和 Rating 使用独立的
  高对比度平台文字色，避免融入渐变背景。
- HTML 与 Pillow 渲染同步更新颜色，布局、列宽、行高和换行规则保持不变。
- 升级图片缓存格式，旧版低对比度图片会自动重绘。

## [1.2.23] - 2026-09-05

### 修复
- 进一步收紧群排行各列的安全边距，确保“近7日变化”始终单行显示且不贴边。
- 普通排行、洛谷排行和总览统一限制成员列宽，长昵称不会挤占指标与变化列。

## [1.2.22] - 2026-09-05

### 修复
- 重新设计群排行的表头和数值列布局，保证“近7日变化”保持单行并留出安全边距。
- 普通平台排行与四平台总览的长指标标题不再贴边或被截断；
  成员、账号、指标和变化值改为清晰的分区式文字回退布局。
- Pillow 回退渲染统一采用单行表头、右对齐数值和列宽限制，避免文字互相覆盖。

## [1.2.21] - 2026-09-05

### 修复
- 修复排行卡字体放大后部分昵称、账号和表头互相贴合的问题。
- Pillow 回退渲染会限制长文本列宽并对长表头分行；HTML 总览会根据昵称和账号
  的换行情况自动增高，避免文字叠层或被截断。

## [1.2.20] - 2026-09-05

### 优化
- 放大普通排行、排行总览和本周进步榜的表头、成员、账号、指标值与变化值，
  并提高文字对比度，改善小字号和模糊问题。
- HTML 与 Pillow 回退渲染同步调整排行列宽、行距和高度估算；
  长昵称、长账号和“当前 Elo / 平台排名”不会挤压其他列。
- 升级排行图片缓存格式，已有旧图片会自动重新生成。

## [1.2.19] - 2026-09-05

### 优化
- 优化群排行、排行总览和本周进步榜的指标表头：
  Codeforces/牛客/AtCoder 显示“当前 Rating”，洛谷按实际数据显示
  “当前 Elo”或“当前平台排名”。
- 洛谷同时存在 Elo 和平台排名时显示“当前 Elo / 平台排名”，不再使用笼统的“当前指标”。

## [1.2.18] - 2026-09-05

### 新增
- 牛客个人资料卡增加题目难度分布、知识点分布、提交通过率、常用语言和近 30/90 天活跃度；
  难度使用牛客题库原生数值区间。
- AtCoder 个人资料卡增加 AtCoder Problems 估计难度分布、ABC/ARC/AGC 等题目系列、
  提交通过率、常用语言和活跃度。
- 洛谷个人资料卡增加公开练习页原生难度分组、题库类型、公开活动日历、资料分项评分和
  Elo 历史分析；练习记录受隐私保护时保留摘要并标注覆盖范围。

### 优化
- 账号分析只在详细资料命令触发，分析结果缓存 12 小时，题目资源缓存 24 小时；
  群排行继续使用摘要资料，不触发题目级扫描。
- HTML、Chromium 和 Pillow 资料卡同步展示分析摘要、分类图表、数据源和统计覆盖范围；
  分析服务临时失败时使用短缓存，避免长时间保留失败结果。

## [1.2.17] - 2026-09-05

### 优化
- 放宽菜单、比赛长消息和 Pillow 回退渲染的字间距、行距与条目留白，
  缓解中文内容过密、阅读拥挤的问题。
- HTML 与 Pillow 使用一致的宽松排版参数，并升级缓存格式，旧版密集图片会自动重绘。

## [1.2.16] - 2026-09-05

### 优化
- 修复菜单、比赛长消息和 Pillow 回退渲染的中文字体选择问题；
  对 Noto Sans CJK TTC 显式使用简体中文字体面，避免出现方框或字形错乱。
- HTML/Chromium、Firefox 与 Pillow 回退统一字体链，并升级图片缓存格式；
  旧字体缓存会在下次发送时自动重绘。

## [1.2.15] - 2026-09-05

### 修复
- 修复洛谷个人介绍为空时无法生成绑定验证码的问题；现在会正常发放验证码，
  用户将验证码追加到个人介绍后即可确认绑定。
- 只有个人介绍字段缺失或洛谷资料读取失败时，才会阻止发放验证码。

## [1.2.14] - 2026-09-05

### 修复
- 修复洛谷绑定把“个人介绍字段存在但为空”和“字段缺失/资料读取失败”
  混淆的问题。
- 个人介绍为空时给出明确填写提示；确认绑定阶段保留有效期内的待确认记录，
  补充验证码后可直接重试，不需要重新开始绑定。

## [1.2.13] - 2026-09-05

### 优化
- 个人资料卡新增最近 8 场 Rating 趋势折线图；Codeforces 资料卡新增通过题难度横向条形图，
  让 Rating、做题量和难度结构更直观。
- Codeforces 难度区间细分为 `≤999`、`1000–1199` 至 `3000–3199`、`3200+`，
  不再把 2400 分以上题目全部合并为一个区间。
- HTML/Chromium 和 Pillow 无浏览器回退渲染同步支持图表；优化中文字体选择、文字抗锯齿
  和卡片高度估算，图表内容不会挤压或裁切其他资料。

## [1.2.12] - 2026-09-05

### 新增
- Codeforces 个人资料卡增加通过题的难度分布，按已通过的不同题目去重统计，
  展示 `≤999` 至 `3200+` 及未标分区间。

### 优化
- 个人资料卡只展示实际存在的统计项，补充通过题数、贡献、最高段位、组织/地区等信息，
  并压缩重复字段和无意义空白。
- 普通群平台排行增加“名次、成员/账号、当前指标、近7日变化”表头。
- 群排行总览和本周进步榜增加对应列标题，并明确进步榜的当前指标列。
- HTML 和 Pillow 渲染保持同一字段顺序与高度估算，升级卡片缓存格式。

## [1.2.11] - 2026-09-05

### 修复
- 洛谷账号资料和绑定验证码改为优先读取 `www.luogu.com/user/{uid}` 页面中的
  `introduction` 字段。
- `luogu.me` 不再作为绑定认证前置条件；`.com` 页面暂时无法访问时仍兼容
  `.com.cn` 资料接口/页面作为兜底。
- `1928724` 这类 `.com` 页面存在个人介绍字段但内容为空的账号，会明确反馈
  个人介绍暂时无法绑定，而不会误判为域名或账号不存在。

## [1.2.10] - 2026-09-05

### 修复
- 洛谷绑定验证码改为读取 `luogu.me` 的稳定公开接口
  `api.luogu.me/user/query/{uid}` 中的 `introduction`。
- 洛谷昵称、Rating、排名、通过题数、头像等其他资料继续使用
  `luogu.com.cn`，不改变个人战绩和群排行的数据来源。

## [1.2.9] - 2026-09-04

### 修复
- 修复 `确认绑定<平台> <非法账号>` 被静默接受的问题；现在会明确提示账号参数格式错误，
  不会继续读取或保存错误账号。
- 修复跨群完成确认时排行归属可能错位的问题；优先保留发起绑定时记录的群。
- 修复多平台个人资料卡、平台排行卡和排行总览卡渲染异常时整条指令无响应的问题，
  统一回退为文字结果。
- 修复快照写入、昵称更新、排行缓存读取等可选步骤异常时误报账号查询/绑定失败的问题。

### 优化
- 绑定成功后的确认指令提示统一使用 `确认绑定cf`、`确认绑定牛客` 等可直接复制的写法，
  保留验证码追加到公开资料字段的说明。
- 头像预加载、浏览器和 Pillow 回退增加异常隔离，避免装饰性头像或 UI 故障影响账号功能。

## [1.2.8] - 2026-09-04

### 修复
- 补充 `绑定cf`、`绑定牛客`、`绑定洛谷`、`绑定atcoder` 缺少账号参数时的用法提示，
  明确显示所需账号类型和验证码对应的公开资料字段。
- 带账号参数的绑定流程及原有验证码提示保持不变。

## [1.2.7] - 2026-09-04

### 修复
- 修正 Codeforces 头像读取：优先使用 API 的 `avatar`，不再误用横向
  `titlePhoto` 作为头像。
- 增加 AtCoder 头像解析，并为洛谷公开头像增加稳定的 UID 兜底地址；
  四个平台统一规范化协议相对/相对路径头像。

### 优化
- 全部卡片、群排行、比赛长消息和后台配置页统一为樱粉、珍珠白、冰蓝、
  花瓣与水晶元素的视觉主题，减少通用 AI 科幻模板感。
- 无浏览器的 Pillow 回退渲染同步使用同一套主题；头像下载后裁剪为正方形并缓存。

## [1.2.6] - 2026-09-04

### 新增
- `我的账号`、`我的战绩` 及各平台个人战绩卡在群聊中增加当前用户的本群排行。
- 个人资料卡增加平台头像缩略图：固定正方形容器并使用裁剪填充，头像不可用时
  回退为平台色占位图。

### 优化
- 单平台卡片放大头像，多平台卡片使用紧凑方形头像，提升缩略图显示观感。
- 私聊个人资料卡不显示群排行；没有群上下文时不触发群排行查询。

## [1.2.5] - 2026-09-04

### 修复
- 修复群排行总览在 Pillow 回退渲染时因分区高度不同导致洛谷、AtCoder
  与 Codeforces、牛客分区互相叠层的问题。
- 总览第二行现在会根据上一行两个分区的最大高度计算位置，并自动清理旧版
  错误卡片缓存。

## [1.2.4] - 2026-09-04

### 修复
- 修复群排行卡截图高度估算偏小导致底部排行行被裁切、视觉上出现叠层的问题。
- 排行卡现在会根据行数、长昵称/账号换行和底部翻页提示动态计算高度，
  HTML/Chromium 与 Pillow 回退路径均可完整显示。

## [1.2.3] - 2026-09-04

### 优化
- 面向大群优化竞赛群排行：平台榜单固定每页最多 30 人，支持页码查询，
  例如 `群cf排行 2`、`群cf排行第2页`。
- `群排行` 和 `本周进步榜` 保留各平台前 5 名总览，并提示使用平台榜单查看完整排行。
- 增加排行结果 5 分钟缓存、缓存上限、分批读取和并发限制，避免多人同时查询时
  重复抓取或一次性创建过多任务。
- Rating 快照改为批量读写，减少 2000 人群排行时的 KV 数据库操作。

## [1.2.2] - 2026-09-04

### 优化
- 优化 `我的账号`、`我的战绩` 及 `我的CF`、`我的牛客`、`我的洛谷`、
  `我的AtCoder` 的个人资料卡布局。
- 单个平台改为整行紧凑卡片，多平台卡片取消无意义的拉伸，图片高度按平台数量
  和资料内容自适应，减少底部空白。
- HTML/PNG 卡片缓存格式升级；修复 Chromium 截图临时文件后缀不正确时无法生成
  PNG 的问题。

## [1.2.1] - 2026-09-04

### 修复
- 修正 Codeforces 账号绑定校验：`Last name/姓氏` 支持与其他平台一样追加验证码，
  不再因追加原有姓氏而验证失败。
- 统一四个平台的绑定提示、校验逻辑与文档：验证码追加到对应公开资料字段，
  字段中包含完整验证码即可通过。

## [1.2.0] - 2026-09-04

### 新增
- 新增 Codeforces、牛客、洛谷、AtCoder 四个平台账号绑定。
- 绑定采用一次性验证码验证：CF 姓氏、牛客个性签名、洛谷个人介绍、
  AtCoder Affiliation（所属）；验证码只保存哈希，10 分钟有效。
- 新增 `我的账号`、`我的战绩`、`我的CF`、`我的牛客`、`我的洛谷`、
  `我的AtCoder`，个人资料以二次元科幻风格 PNG 卡片展示。
- 绑定成功后自动加入发起绑定的群排行；新增 `群排行`、各平台排行、
  `本周进步榜`、`退出群排行`。
- 新增账号资料缓存、Codeforces 请求间隔控制、公开数据失败兜底和卡片缓存。
- 洛谷个人页无法读取或个人介绍不可见时，明确提示“暂时无法绑定”。

## [1.1.27] - 2026-09-04

### 新增
- 新增 `最近比赛`（`近期比赛`）指令，汇总牛客、Codeforces、AtCoder、洛谷和
  XCPC 线下赛的近期赛程，并按开始时间统一排序。
- WebUI 新增“最近比赛查询范围”，默认未来 7 天，可设置 1～30 天，保存后立即生效。
- 各平台查询并发执行并复用现有缓存；线下赛结果继续标注 XCPC Link 数据源。

## [1.1.26] - 2026-09-04

### 新增
- WebUI“推送设置”新增文字转图片阈值配置：可分别设置最大字符数和最大行数。
- 配置保存后立即生效，无需重启插件；缺失或异常的旧配置自动使用默认值。
- 后台提交值增加范围校验，避免设置过小导致普通消息频繁转图或设置过大超过平台限制。

## [1.1.25] - 2026-09-04

### 优化
- acmer 群管理插件的菜单、比赛列表、线下赛列表、刷新结果和主动推送统一采用自适应输出：
  内容较短时发送文字，内容较长时生成 HTML 并转为 PNG 图片。
- 图片按消息内容哈希缓存；相同内容后续直接复用已有图片，不重复生成。
- 转图在后台线程执行，不阻塞 AstrBot 事件循环；支持 Chromium/Chrome、Firefox、
  `wkhtmltoimage`，没有浏览器时使用 Pillow，全部不可用时自动拆分为文字发送。

## [1.1.24] - 2026-09-04

### 优化
- 线下赛查询增加 30 分钟内存缓存与插件 `data/contest_cache.json` 持久化缓存，
  插件重载后可直接复用上次成功抓取的数据。
- 增加同一平台的并发请求合并，避免多个群同时查询时重复抓取；网络临时失败时
  优先使用旧缓存兜底。
- XCPC Link 的动态 JS chunk 改为分批并行下载，降低首次抓取等待时间；仍然只
  使用 HTTP 请求，不依赖 Chrome。

## [1.1.22] - 2026-09-04

### 修复
- 查询指令统一忽略英文大小写，`XCPC线下赛`、`xcpc线下赛` 及带 `/` 前缀的写法
  均可触发；中文指令仍保持全匹配。

## [1.1.21] - 2026-09-04

### 修复
- 插件内部改用相对包导入，隔离 AstrBot 重装/重载时可能残留的顶层 `src`
  模块缓存，避免新旧版本混用导致导入错误。

## [1.1.20] - 2026-09-04

### 修复
- 主程序不再依赖 `src/contest_fetcher.py` 导出的新增常量，兼容 AstrBot
  安装过程中插件文件暂时不同步的情况，避免再次出现导入错误。
- 检测到旧版抓取器时，线下赛指令会提示完整重装插件，而不是导致插件加载失败。

## [1.1.19] - 2026-09-04

### 修复
- 兼容 AstrBot 插件更新过程中 `main.py` 与 `src/models.py` 暂时不同步的情况，
  避免启动时报 `cannot import name 'OFFLINE_PLATFORM'`。

## [1.1.18] - 2026-09-04

### 新增
- 新增 `线下赛`（`线下比赛`、`XCPC线下赛`）查询，从 XCPC Link 首页赛程数据中
  提取 ICPC/CCPC 等线下赛事，自动过滤网络赛。
- 支持 `xcpc.link` 主站和 `xcpc.ink` 备用域名，查询结果明确标注实际数据源。

## [1.1.17] - 2026-08-20

### 修复
- 兼容 QQ 官方指令面板自动添加的 `/` 前缀；`/nk比赛`、`/acm菜单`、
  `/acmer激活` 等指令现在与无前缀写法一致。

## [1.1.16] - 2026-08-17

### 修复
- `repo` 显式指定 `tree/master`：WebUI 更新不再依赖 `api.github.com`
  查询默认分支，避免回退到 `main` 导致 404。

## [1.1.15] - 2026-08-17

### 修复
- 修复赛前 15 分钟提醒只推送到一个群的问题：去重键改为按群隔离，
  同一场比赛每个启用群都会各收到一次提醒。

## [1.1.14] - 2026-08-16

### 变更
- `/acm` 快捷菜单改为 `/acm菜单`（`比赛帮助` 别名保留）。

## [1.1.13] - 2026-08-16

### 修复
- 测试推送在“今日无比赛”时不再失败，自动改发最近一场比赛作为测试内容。

## [1.1.12] - 2026-08-16

### 变更
- 移除菜单里的“菜单 ─ 查看聚合菜单导航”条目，三个插件互相独立；
  聚合菜单统一由“菜单导航”插件负责。

## [1.1.11] - 2026-08-16

### 变更
- 修正菜单项：补充“显示本菜单”条目与全匹配/`/` 前缀提示，
  “最近一场”措辞统一为“最近一场比赛”。

## [1.1.10] - 2026-08-16

### 变更
- 菜单展示优化：分区图标、命令分隔符与分隔线排版，并增加“菜单”导航入口。

## [1.1.9] - 2026-08-16

### 变更
- 菜单末尾附带插件开源仓库链接。

## [1.1.8] - 2026-08-16

### 新增
- 新增 `menu.md`：供“菜单导航”插件自动聚合本插件菜单到 `菜单` 指令下。

## [1.1.7] - 2026-08-16

### 变更
- 指令改为全匹配：`nk比赛`、`cf比赛`、`atc比赛`、`lg比赛` 及
  `最近X比赛` 系列；消息必须完整等于指令才会触发，不再前缀匹配，
  避免“牛客xxxx”等聊天内容误触发。

## [1.1.6] - 2026-08-16

### 变更
- 启动日志与配置文档补充“安装后无响应”的排查指引：检查 AstrBot
  设置 → 插件配置 → 可用插件（`plugin_set`）是否包含本插件，
  避免被消息分发白名单过滤。

## [1.1.5] - 2026-08-16

### 修复
- `metadata.yaml` 补充 `repo` 仓库地址，修复 WebUI“更新插件”报
  “does not specify a repository URL or download URL”的问题，
  现在可以直接在 AstrBot 插件管理页一键更新。

## [1.1.4] - 2026-08-16

### 变更
- 指令分发重构为与表情包插件一致的手动前缀匹配（不再依赖
  `@filter.command`）：群聊直接发送 `nk`、`cf`、`最近cf` 等即可触发，
  `/` 前缀和 @机器人 同样可用；降低对特定 AstrBot 版本的依赖。
- `astrbot_version` 下限放宽到 `>=4.13.0`，与表情包插件保持一致。

## [1.1.3] - 2026-08-16

### 变更
- 明确标注 @全体成员在 QQ 官方群聊不生效：日志改为“仅兼容尝试”，WebUI
  与配置文档补充说明（即使机器人是群管理员也无法真正 @ 到人）。

## [1.1.2] - 2026-08-16

### 变更
- 牛客查询指令 `/nc` 改为 `/nk`，`/最近nc` 改为 `/最近nk`
  （中文别名 `牛客`、`最近牛客` 不变）。

## [1.1.1] - 2026-08-16

### 修复
- 修复 AtCoder 比赛数据不全的问题：牛客比赛日历对 AtCoder 收录不完整
  （实测 9 月起为空），改为直接解析 AtCoder 官网 Upcoming Contests 赛程表，
  保证全部未开始比赛都能查到。

## [1.1.0] - 2026-08-16

### 变更
- `/nc`、`/cf`、`/atc`、`/lg` 改为查询各平台**全部未开始比赛**列表
  （最多显示 30 场，超出会提示总数）。

### 新增
- 新增 `/最近nc`、`/最近cf`、`/最近atc`、`/最近lg`（及中文别名），
  只返回各平台最近一场比赛。

## [1.0.9] - 2026-08-16

### 变更
- `acmer激活` 激活成功后，再次发送将静默忽略、不再回复确认消息。

## [1.0.8] - 2026-08-16

### 变更
- 激活状态改为持久保存：`acmer激活` 成功后无需重复触发；AstrBot 重启后，
  只要该群内出现任意一条消息，插件会自动重新激活并记录日志。

## [1.0.7] - 2026-08-16

### 新增
- 新增 `acmer激活`（`/激活`）命令：AstrBot 重启后，群内发送一次即可激活
  本群主动推送（QQ 适配器重启后会清空会话缓存，需要任意一条群消息重新激活；
  机器人回复时会一并缓存消息 ID，激活后推送更稳定）。

## [1.0.6] - 2026-08-16

### 新增
- WebUI 群配置新增“测试推送”按钮：立即向指定群发送今日比赛早报，
  方便验证主动推送链路（发送前需该群先给机器人发过一条消息）。

### 修复
- 主动推送前检测 QQ 适配器会话是否就绪：会话未缓存时不再误报“已发送”，
  而是明确提示“请先让群内发一条消息”。

## [1.0.5] - 2026-08-16

### 新增
- 主动推送的平台实例 ID 改为运行时自动获取（多实例场景按群记录实际来源），
  WebUI 显示检测到的平台 ID 与主动推送前提说明，无需手动配置。

### 修复
- 早报/提醒发送失败时不再标记“已发送”，下个周期自动重试。

## [1.0.4] - 2026-08-16

### 修复
- 修复群配置解析失败（`group_id` 重复传参导致 pydantic 报错）导致定时任务
  读不到任何群、无法推送的问题。
- 每日早报现在包含“今天已开始但仍在进行”的比赛，避免晚于比赛开赛时间
  触发早报时误报“今日无比赛”。

## [1.0.3] - 2026-08-16

### 修复
- 修复 AstrBot 加载插件时 `from src...` 子包导入失败的问题
  （`No module named 'src'`）：插件加载时自动把插件根目录加入 `sys.path`。

## [1.0.2] - 2026-08-16

### 文档
- 新增 `docs/CONFIG.md` 配置文档：配置项一览、管理员权限、推送行为、
  数据源缓存与常见问题。
- 更新日志补充“更新方式”说明。

## [1.0.1] - 2026-08-16

### 新增
- 新增功能菜单 `acmer群管理插件菜单`：群聊直接发送即可查看全部指令与所需
  权限（无需 @机器人）；原 `/acm`、`比赛帮助` 保留为快捷方式。

## [1.0.0] - 2026-08-16

### 新增
- ACM 竞赛工具插件：牛客 / Codeforces / AtCoder / 洛谷比赛查询，
  每日早报、赛前 15 分钟提醒，WebUI 配置页与管理员校验。

## 更新方式

1. `git pull` 拉取最新代码；
2. 把 `main.py`、`metadata.yaml`、`requirements.txt`、`src/`、`pages/`
   同步到 `AstrBot/data/plugins/acmer_qq_group_bot/`；
3. 在 AstrBot WebUI 插件管理页重载插件（或重启 AstrBot）。
