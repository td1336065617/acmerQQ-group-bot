# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更 · ⚠️ 升级注意

最近版本逐条记录；更早的版本按功能阶段合并归类，其中每个版本号仍保留原始日期与要点。

---

## 近期版本

## [1.19.6] - 2026-09-26

> 🐛 修复：协议端掉线/无发送权限时，推送每轮重试 —— 5 分钟内 126 条失败日志 + 进程 CPU 打满（生产实测）。

### 🐛 修复
- **推送失败退避**：同一群连续失败 3 次后进入 15 分钟冷却（连发送动作都不再做），
  成功一次即清零。冷却与计数走 KV（key 前缀 pushfail_group_），重启后仍生效。
- **告警限流**：「主动推送会话未就绪」这类每轮都会出现的提示，改为同一群 10 分钟最多打一次。
- 冷却触发时打一条明确日志（提示检查协议端登录与发送权限），便于定位。

### ✅ 验证
- 新增 3 条用例（连续失败进冷却、成功清零、会话未就绪不进计数且限流）；acmer 全量 389 条通过。

---

## [1.19.5] - 2026-09-26

> 🐛 修复：赛后赛果巡检在「本群绑定人数不足」时空转 —— 每个 tick 重复评估同一场，
> 造成重复抓取 + 日志洪水 + AstrBot 进程 CPU 打满（生产实测）。

### 🐛 修复
- 「成员不足」跳过时**也落幂等标记**（此前只跳过不落标记，于是 2 小时窗口内每 30 秒重评一次）。
- 标记里带**成员指纹**：之后有人绑定该平台时指纹变化，仍会补评估一次并正常推送
  —— 保留「稍后有人参加就能补推」的原有语义（与 1.13.3 赛果缓存串台同一修法思路）。
- 跳过会写一条推送日志（kind=settle，detail 记明人数与阈值），便于后台查询。

### ✅ 验证
- 新增/改写 2 条用例（跳过落标记、成员变化后补推、未就绪时不写键）；
  acmer 全量 386 条用例通过。
- 生产实测依据：同一场比赛的「跳过」日志在 10 分钟内出现 570 次，进程 CPU 持续 100%。

---

## [1.19.4] - 2026-09-26

> ⚙️ 补回“人 → 群”视图：编辑抽屉只读显示「当前已在…」（1.19.2 一并被删掉的那块）。

### ✨ 新增
- 「账号绑定 → 编辑」抽屉新增**只读提示**：`当前已在：群A（自然成员）、群B（强制加入）（查看与调整请在「群排行 → 排行成员」）`；没有任何群时显示“该用户当前不在任何群的排行榜里”。

### ⚙️ 变更
- 明确两个视角**互补、不重复**：**群 → 人** 看「群排行 → 排行成员」；**人 → 群** 看绑定编辑抽屉（只读）。设置入口仍只在**新增**绑定时的「归属群」下拉。
- 绑定列表继续不展示「归属群」列；数据仍由 `GET bindings` 的 `groups` 返回，供上面的只读提示使用（该字段**保留**）。

### 🧪 验证
- 无头冒烟：新增态显示下拉（不加入 + 已注册群）、编辑态隐藏输入框并显示“当前已在…”、列表无该列、账号回填 `platform_user_id`、0 JS 错误。

---

## [1.19.3] - 2026-09-26

> ⚙️ 按反馈调整：新增绑定时选的归属群改回"普通加入"（用户之后可自行退出）。

### ⚙️ 变更
- 新增绑定时的「归属群」**改回老路径**：`save_binding(group_id=…)` → `group_members.enabled=1`，属于"管理员顺手加入"，用户在群里发「退出排行」即可自行退出；响应里不再有 `membership_added`。
- 需要**强制且留痕**的成员（成员自己退不掉、可在列表里识别来源并移除）仍走「群排行 → 排行成员」。
- 1.19.2 的职责归位不变：抽屉「归属群」只在**新增态**出现；绑定列表**不展示**归属群；编辑不携带 `group_id`（编辑永不扰动成员资格）。

### 🧪 测试
- 回滚相关断言（`save_binding` 重新接收 `group_id`）；"不带归属群"用例改为断言传入 `None`。

---

## [1.19.2] - 2026-09-26

> ⚙️ 后台「归属群」职责归位：新增绑定时可设置，编辑与展示统一交给「群排行 → 排行成员」。

### ⚙️ 变更
- 「账号绑定」抽屉里的**「归属群」只在新增时出现**（编辑态隐藏）——编辑只负责账号本身；绑定列表**不再展示「归属群」列**。成员归属的查看、加入与移除统一在「群排行 → 排行成员」。
- 新增时选的归属群**不再走老路径**（那里直接 `enabled=1`、无留痕、用户自己发「退出排行」就能掉），改为 `add_rank_member(added_by="webui:binding")` 写入覆盖表：在「排行成员」里显示为**强制加入**、移除时按 `preexisting` 还原；接口响应新增 `membership_added`。
- 若加入群排行失败：绑定本身已保存，但会明确提示"请到「群排行 → 排行成员」重试"，不静默失败。
- 1.19.1 引入的编辑态「保持不变（不改成员资格）」下拉随本次重构移除；编辑绑定不再携带 `group_id`，**编辑永远不会扰动群排行成员资格**。
- 群排行页「排行成员」卡片文案改为"成员归属在这里统一管理（新增绑定时可顺带指定；之后的查看、加入与移除都在这里）"。

### 🧪 测试
- 新增 2 条：不带归属群时不动成员资格；加群排行失败时的提示语义（绑定已写入 + 引导去排行成员）。
- 更新后台接口用例以匹配新语义（`save_binding` 不再收 `group_id`，改断言覆盖表调用）。

---

## [1.19.1] - 2026-09-26

> 🐛 修复后台「编辑绑定」：牛客/洛谷保存必失败、只改昵称也要联网校验、误报"已换绑"，以及归属群显示。

### 🐛 修复
- **牛客 / 洛谷的绑定在后台无法编辑**：编辑表单回填的是 `handle`（展示名），而后端需要的是 `platform_user_id`（如牛客 `645160704`、洛谷 `273293`）；`normalize_account_identifier` 对展示名返回空 → 保存直接报"请填写数字 UID"。线上 32 条牛客/洛谷绑定（这两个平台的 **100%**）全部受影响。现在编辑态回填 `platform_user_id`，并在字段旁标注展示名；后端同时把"输入恰好等于当前值"视为未改账号，兼容尚未刷新的旧页面。
- **只改昵称也要重新校验、平台抖动时改不了**：save 分支原无条件 `get_profile(force=True)`。现在账号归一化后未变 → **跳过校验与抓取**，只更新昵称，并沿用原 `verified_at`（不谎报校验时间）。
- **编辑任何已存在绑定都提示"已换绑，替换原账号 X"**（X 就是它自己）：`replaced` 原来无条件取旧 handle。现在仅在账号**真的变化**时返回；前端据此区分"已换绑"与"已保存（账号未变更，未重新校验）"。
- **「归属群」在编辑时被清空**：看起来像不在任何群排行里，顺手选一个群保存还会把成员**强制加入**该群。现在编辑态默认项为「保持不变（不改成员资格）」，并显示"当前已在：群A（自然成员）、群B（强制加入）"；绑定列表新增「归属群」列。
- **编辑态可改「平台 / QQ 用户 ID」→ 产生"旧条残留 + 新条插入"的幽灵绑定**：现在编辑态锁定这两个字段并给出提示；如需更换请删除后重新新增（删除只删绑定行，**不影响**群成员资格与「排行成员」记录）。

### ⚙️ 变更
- `GET /bindings` 条目新增 `groups`（该用户已在的群排行，一次批量查询，避免 N+1）；`POST /bindings` 响应新增 `reused`，`replaced` 语义收紧为"真的换绑"。
- 新增 8 条回归测试（`tests/test_bindings_edit.py`）。

---

## [1.19.0] - 2026-09-26

> ✨ 后台「群排行」新增成员管理：可把已绑定用户强制加入某个群的排行榜。

### ✨ 新增
- **后台强制加入排行**（群排行页 →「排行成员」卡片）：选群 → 搜索已绑定用户（QQ / 账号 / 昵称）→ 强制加入；成员表区分「自然成员」与「强制加入」，移除只针对强制加入的标记。
- 与用户自己在群里「加入排行」的区别：强制加入单独落库（新表 `rank_member_overrides`），读取群成员时 UNION 进来，因此用户之后发「退出排行」或退群都**不会掉**；移除时只去掉强制标记，本来就在排行里的成员保持原状（`preexisting` 记录）。
- 新增接口 `GET /acmer_qq_group_bot/rank/members`、`POST /acmer_qq_group_bot/rank/members`（`action=add|remove`，成功后会失效该群排行缓存）。
- 新增 7 条测试：覆盖表 UNION 语义、用户退出后仍保留、账号来源带上强制成员、移除还原、来源标记、registry 流程与 KV 回退。

### ⚙️ 变更
- 群排行成员来源改为 `group_members(enabled=1)` ∪ `rank_member_overrides`（影响 `get_group_member_ids` 与 `get_group_platform_accounts`）。

---

## [1.18.0] - 2026-09-26

> ✨ 后台改为「群名优先」，官方渠道也能看到群名；🐛 顺带修复「保存群配置」一直失败。

### 🐛 修复
- **后台「保存群配置」永远失败**：前端整行回传的群对象自带 `group_id`，而 `_build_groups_payload` 又写了一遍
  `GroupConfig(group_id=gid, **item)` → `TypeError: got multiple values for keyword argument 'group_id'`，
  接口只会返回“群 xxx 配置不合法”。现在先剔除 `group_id` 再构造，保存恢复正常（群名等字段也随之保留）。

### ✨ 新增
- **群名缓存与补全**（`src/group_names.py` + `GroupConfig.name`）：
  - 被动：OneBot 事件自带 `group_name`，随 `remember_group` 写入（同会话同群名不重复写 KV）；
  - 主动：后台新增**「刷新群名」**按钮 → OneBot 走 `get_group_info`，**官方走开放接口**
    `GET /v2/groups/{group_openid}/info`（复用 botpy 已持有的 access_token，无需另配 appid/secret）；
    新增接口 `POST /acmer_qq_group_bot/groups/refresh-names`。
- 后台展示统一为「群名 + ID 简写」（悬停看完整 ID）：群推送配置表、群选择器、概览「下次推送」、推送日志；搜索支持群名。

---

## [1.17.3] - 2026-09-26

> 🐛 修复：官方通道“必须先 @机器人 才能唤醒”时，正文里残留的机器人 @ 会让指令匹配失败。

### 🐛 修复
- QQ 官方把“@机器人”的消息作为**独立事件类型**（`GROUP_AT_MESSAGE_CREATE`）投递，适配器会强制补一个机器人 At；某些形态下该占位符 @ 会残留在正文（例如 `<@qq_official> 绑定cf xxx`）。而绑定/确认/解绑/查询指令的正则都锚定 `^`，这类消息会**完全静默**（既不自助绑定，也不代绑定）。
- 现在匹配指令前统一清掉所有 @ 记号（`<@id>` / `<@!id>` / `[CQ:at,qq=]` / `[At:...]`），因此官方场景下：
  - `@机器人 绑定cf xxx` → 自助绑定；
  - `@机器人 @目标 绑定cf xxx` → 代绑定该目标（需后台管理员）；
  - `@机器人 我的战绩` → 按“查自己”处理。
- 新增 3 条回归用例覆盖上述场景。

---

## [1.17.2] - 2026-09-26

> 🐛 修复：@了多个成员 + 绑定指令时会退回“给发送者自助绑定”，可能把别人的账号绑到自己名下。

### 🐛 修复
- 被 @ 目标多于一个时，`_mentioned_admin_bind` 返回 `None`，上层于是**退回自助绑定**：管理员本想代绑被 @ 的成员，却拿到一张绑到**自己**名下的验证码，确认后就把对方的 OJ 账号绑到了自己身上。
- 现在「多目标 + 绑定指令」会明确回复“检测到 N 个被 @ 的成员，无法确定要绑定谁”，**不再退回自助绑定**；「多目标 + 非绑定指令」仍然忽略（保持原行为）。
- 新增 2 条回归用例，并把原「多 @ 返回 None」的断言更新为新语义。

---

## [1.17.1] - 2026-09-26

> 🐛 修复：@机器人 + 绑定指令被误判为“代绑定他人”，非管理员收到“此指令仅限管理员”。

### 🐛 修复
- QQ 官方通道拿不到“这条消息 @ 的是机器人自己”的标记时，适配器会把 `self_id` 与消息链里的 At 塞成占位符 `qq_official`。此前该占位符被当成**真实被 @ 的成员**，于是机器人自己成了“管理员代绑定”的目标：非管理员在群里 @机器人 发 `绑定cf xxx` 会收到“此指令仅限管理员”，**自助绑定流程根本走不到**。
- 现在占位符（`qq_official` / `qq_official_webhook` / `unknown_selfid` / `unknown` / `self` / `bot` / `all` / `everyone`）在三条解析路径（`mentions` 列表、AstrBot 消息链 `At`、`<@id>`/CQ 文本回退）全部排除：`@机器人 + 绑定cf xxx` 正确回到自助绑定（发验证码）。
- 新增 3 条回归用例覆盖该线上场景。

---

## [1.17.0] - 2026-09-25

> 🎨 后台 UI 重设计 + 账号绑定可视化管理。

### ✨ 新增
- 后台改为「左侧导航 + 分区」共 7 区：概览与状态 / 基础设置 / 推送设置 / 群推送配置 / 账号绑定 / 群排行 / 配置备份。
- **账号绑定增删改查**：后台可直接新增 / 换绑 / 删除绑定；与群内 `@某人 绑定cf` 共用同一份数据，同样跳过验证码、同样受“同一 OJ 账号全局唯一”约束（不顶号），保存 / 删除后立即失效群排行缓存。
- **运行状态面板**：群数 / 管理员数 / 存储后端 / 平台实例、下次推送时间、最近推送结果与失败原因。
- **立即试跑**（单群）：早报（含周榜）/ 训练周报 / 赛后赛果 / 报名提醒；**不写幂等键**，不影响当天正式推送。
- **配置导入 / 导出**（JSON）：导入先返回差异预览，确认后才落库。
- 群推送配置支持搜索、分页、批量启用/禁用、批量测试与行内编辑与“还原本行”。
- 推送设置拆成 7 组，分区独立保存 + 未保存提示 + 字段级校验。

### ⚙️ 变更
- 新增 KV 键 `push_log`（环形保留最近 200 条）记录推送结果。
- 周报 / 赛果 / 报名提醒的发送逻辑抽出可复用函数（`write_key` 控制是否写幂等键），原有行为保持不变。

---

## [1.16.1] - 2026-09-25

> 🐛 管理员 @代绑定：手打 @昵称 时给出提示，不再静默无反馈。

### 🐛 修复
- 手打的 `@昵称` 只是普通文字，QQ 不会提供对方 openid；此前这类消息会被静默忽略，管理员会以为“指令没生效”。现在会回复提示，说明必须用 QQ 的 @ 从成员列表选择成员。
- 权限不足时同样回复“此指令仅限管理员”，避免误判为无响应。

### ⚙️ 变更
- README / CONFIG 补充：代绑定的 @ 必须是真正的成员 @。

---

## [1.16.0] - 2026-09-23

> ✨ 管理员 @代绑定：后台管理员可直接把竞赛账号绑定到被 @ 的成员名下。

### ✨ 新增
- 管理员在群内发送 `@某人 绑定cf <账号>`（支持 `代绑定` 前缀与四个平台），跳过验证码直接写入被 @ 成员名下，并自动加入本群排行。
- 权限口径：仅 WebUI「管理员设置」`admin_users` 生效；QQ 群主 / 群管理员身份不构成管理员权限。
- 覆盖目标已有绑定时提示“已替换原有绑定”；账号已被他人绑定时明确拒绝，不顶号。

### ⚙️ 变更
- 菜单「仅管理员」区新增代绑定用法；README / CONFIG 补充授权口径说明。

---

## [1.15.2] - 2026-09-23

> 🐛 Windows 兼容 + 缓存目录迁出插件代码目录。

### ✨ 新增
- 浏览器 / 中文字体 / Emoji 字体候选新增 Windows 常见路径（Chrome / Edge / Firefox、msyh.ttc 等、seguiemj.ttf）。
- 新增 src/plugin_paths.py：缓存统一落在 data/plugin_data/acmer_qq_group_bot/cache/，启动时把旧版写在插件目录 data/ 下的缓存**复制**过来（不删除原文件，可回滚）。

### ⚙️ 变更
- 缓存目录由「插件代码目录」改为「AstrBot 插件数据目录」，避免 Windows 只读安装位置写入失败、以及插件更新时缓存被清空。

---

## [1.15.1] - 2026-09-22

> 📝 账号绑定/查询提示补充具体示例，降低上手门槛。

### ⚙️ 变更
- 绑定 / 确认绑定 / 解绑 / 查询的用法提示都加了一行「例如：…」（CF 示例用 jiangly）。
- 「我的账号」未绑定时的帮助、以及插件菜单同步补充示例。

---

## [1.15.0] - 2026-09-22

> QQ 双通道适配：支持 OneBot v11（aiocqhttp），官方族行为零回归。

### ✨ 新增
- 新增 platform_compat.py：平台判定、平台实例解析、主动推送就绪判定、@全体适配。
- 群配置按 platform_id:group_id 作用域存储，并持久化 umo 用于主动推送；旧数据读取时自动迁移。

### ⚙️ 变更
- support_platforms 增加 qq_official_webhook、aiocqhttp。
- OneBot 主动推送不受被动窗口限制；@全体使用 At(qq="all")。

### 🧪 真机验证
- 已用 NapCat（OneBot v11）验证：群配置按 platform_id:group_id 存储、umo 持久化，WebUI 测试推送经 context.send_message(umo) 成功送达。

---

## [1.14.14] - 2026-09-18

> 🖼️ 提升 \`CARD_FORMAT_VERSION\`（18 → 19）：改了排名文案但没提版本号，**旧卡片图片被缓存复用**，
> 导致资料卡上「（榜单快照）」标注看不到。

### 🐛 修复
- 卡片图片按内容签名缓存，签名里含 \`CARD_FORMAT_VERSION\`；1.14.12/1.14.13 只改了渲染文案，
  版本号没动 → 命中旧图（没有标注）。现在提升到 19，旧图全部失效、重新渲染。

---

## [1.14.13] - 2026-09-18

> 🏷️ 把「（榜单快照）」标注真正接到排行卡上（上一版只在文案函数里改了，渲染时拿不到平台）。

### 🐛 修复
- 排行卡渲染时行数据里没有 \`platform\` 字段，导致 \`_platform_rank_text()\` 判断不出是 CF，标注不显示。
  现在 \`render_ranking()\` 支持 \`platform\` 参数（四平台总览卡按分区自动注入），
  \`main.py\` 调用处透传 \`mode\`，并把 \`platform\` 注入到每一行。

---

## [1.14.12] - 2026-09-18

> 🏷️ CF 平台排名标注「（榜单快照）」，避免与官网实时名次对不上时被当成 bug。

### ⚙️ 变更
- CF 名次来自官方全站榜单（\`user.ratedList\`）的**周期快照**，榜单里的 rating 会滞后于实时
  （实测 BenQ 实时 3857 / 榜单 3650），因此名次可能与官网有少量差异。
  现在文案显示为 \`平台排名 #4（榜单快照） · Top 0.00%\`，把数据来源说清楚。
- 标注紧跟名次（不放在百分位之后），这样窄列丢弃百分位时**标注不会一起被丢掉**。

---

## [1.14.11] - 2026-09-18

> 🐛 修掉「多个人都显示平台排名 #1」——旧算法的错误名次被资料缓存固化了。

### 🐛 修复
- `_fill_codeforces_ranks()` 原来遇到「已有 `rating_rank` 的资料」就跳过，
  而资料缓存里存的正是**上一版错误算法**算出来的名次（高于榜单最高分的一律为 1），
  于是缓存命中时错误名次一直不纠正——线上表现为「好几个人都显示 #1」。
- 现在改为**每次都用榜单位次覆盖**，并清掉榜单里查不到的人的名次，
  缓存里的脏值会在下一次取数时被纠正。

---

## [1.14.10] - 2026-09-18

> ⏳ CF 全站榜单：超时放宽到 **10 分钟** + 失败重试 + 旧榜单兜底。

### ⚙️ 变更
- 超时 240s → **600s**：全量榜单 13.7 万人本身就要 100 秒以上，
  且**绑定人数会持续增长**、CF 还会限速，给足余量（它是启动后台预热 + 24 小时缓存，
  慢一点不影响用户查询）。
- **失败重试 3 次**（立即重试，不退避等待）：CF 偶发 502/超时。
- **拉取失败回退旧榜单**：以前失败就返回空列表 → 所有 CF 名次消失；
  现在改用磁盘上的旧榜单兜底（名次可能略旧，但不会整列空掉）。

---

## [1.14.9] - 2026-09-18

> ⏱️ CF 全站榜单改为**启动后台预热**，并把超时放宽到 240 秒。

### 🐛 修复
- **CF 排名在线上始终为空**：全量榜单实测 13.7 万人 / 约 **115 秒**，而超时被设成 90 秒，
  请求必然超时 → 位次表为空 → 一个人都拿不到名次（线上实测：53 行只有 4 行有值）。
  超时放宽到 240 秒。
- **不让用户查询等两分钟**：插件启动后在后台预热榜单（`initialize()` 里起异步任务），
  用户查询时直接命中缓存。

---

## [1.14.8] - 2026-09-18

> 🎯 CF 平台排名改用「榜单位次」，修掉顶尖选手名次错乱（jiangly / BenQ 都显示第 1）。

### 🐛 修复
- **CF 排名算错**：原实现用「实时 rating 去榜单里二分」，但 \`user.ratedList\` 里的 rating
  是**过期快照**（实测 BenQ \`user.info\`=3857 / \`ratedList\`=3650，tourist 3528/3301），
  于是 rating 高于榜单最高分的选手（如 jiangly）一律被算成第 1 名。
  现在直接取**该 handle 在榜单中的位次**作为名次（榜单本身即排名）。
  实测：jiangly #1、BenQ #4、tourist #16、Petr #137（共 137134 人）。
- **榜单改用全量**：原请求带 \`activeOnly=true\`，会漏掉不活跃的顶尖选手
  （37992 人、最高分 3655；全量为 137143 人、最高分 3810）。旧缓存带 \`source\` 标记，自动失效。
- 百分位极小值不再显示成 \`Top 0.00%\`，改为 \`Top <0.01%\`。

---

## [1.14.7] - 2026-09-18

> 🧭 对着菜单逐条跑了一遍全命令测试，修掉 2 个「菜单里有、发了没反应」的死项。

### 🐛 修复
- **`确认绑定` / `解绑`（不带平台）此前完全没回复**：菜单里就是按这种写法列的，
  但正则要求必须带平台名，于是裸发这两条既不报错也不回话。
  现在会回用法提示（`用法：确认绑定cf <验证码>` / `用法：解绑cf …`），
  带平台的写法仍走正式流程。

### ⚙️ 变更
- 全命令巡检结论：acmer 菜单 45 条（拆开别名后）其余全部有处理器；
  菜单导航 4 条、QQ群管理 42 条、表情包管理 13 条**全部正常**。

---

## [1.14.6] - 2026-09-18

> 🐛 CF 排名的最后一处遗漏：**批量取数路径**。

### 🐛 修复
- 群排行对 Codeforces 走的是**批量 `user.info`**（一次请求取全群），该路径直接构造资料、
  不经过单账号路径，所以上一版补的 CF 排名对它无效——线上实测重算后 **53 行只有 1 行有排名**，
  而同期牛客 13/21、洛谷 9/14、AtCoder 8/9 都正常。
- 现在抽出 `_fill_codeforces_ranks()`，**批量与单账号路径共用**（批量时只读一次落盘的全站榜单，
  再对每人二分定位，不会产生额外网络请求）。

---

## [1.14.5] - 2026-09-18

> ⏱️ 重载/升级后**立即作废旧排行快照**，不用再等 1 小时。

### 🐛 修复
- **升级后群里排行长时间不变**：排行快照新鲜期是 **1 小时**（`RANK_SNAPSHOT_MAX_AGE`），
  插件重载后仍会直接返回旧快照、不重算，于是「所有排行都还是老版本」——
  线上实测：23:58 生成的快照到 00:58 前都被视为新鲜，期间查询不会刷新。
  现在插件启动/重载时会把全部排行快照标脏（`mark_all_rank_dirty()`），
  第一条排行查询就会重算并带上平台排名。

---

## [1.14.4] - 2026-09-18

> 🐛 修复群排行看不到平台排名的根因：**CF 的排名只算在详细资料分支**。

### 🐛 修复
- **CF 平台排名在群排行里始终为空**：群排行/快照用的是**轻量资料**（`detail=False`，为速度考虑），
  而 CF 的全站排名计算被写在 `detail=True` 分支里，于是快照里 `rating_rank` 永远是 NULL
  （线上实测：rank_snapshot 四平台 97 行全部为空）。现在轻量资料也会算排名。

### ⚙️ 变更
- 说明：资料缓存 TTL 为 30 分钟，牛客/洛谷/AtCoder 的旧缓存里没有排名字段，
  **最长 30 分钟**后自动补齐；CF 属于代码缺陷，必须升级本版本才会出现。

---

## [1.14.3] - 2026-09-18

> 🧩 排行卡也带上百分位，并修掉三处可能「排版炸掉」的隐患。

### ✨ 新增
- **群排行卡显示「平台排名 #N · Top X%」**：快照表新增 `rating_rank_total` / `rating_rank_note` 两列
  （幂等迁移），渲染时复用资料卡的 `_platform_rank_text()`，因此资料卡与排行卡口径完全一致。

### 🐛 修复
- **窄列不再截成「#1200 · Top 0…」**：Pillow 路径在放不下组合值时，改为退回只显示名次
  （`#1200`），而不是把百分位截断成省略号。
- **资料项不再换行撑破栅格**：HTML 路径给资料项的值与标签加 `white-space:nowrap` + 省略号兜底。
- **AtCoder 段位不再重复平台排名**：用户页的 `Rank` 行本来是「全站名次（百分位）」，
  以前被当成段位显示，导致窄列里出现 `48t…` 这种截断；现在段位改用颜色类，
  并映射成中文（`red → 红`、`blue → 蓝`…），名次与百分位只出现在「平台排名」一项。

---

## [1.14.2] - 2026-09-18

> 🏅 群排行卡（平台分榜与四平台总览）的每个成员都显示平台内排名。

### ✨ 新增
- **群排行卡显示平台排名**：成员名下方的小字行追加「平台排名 #48」，与资料卡共用
  `_platform_rank_text()` 文案；HTML 与 Pillow 两条渲染路径、平台分榜与四平台总览
  四种组合全部覆盖。没有名次的成员不显示占位符（不会出现「#None」）。
- 排行文字兜底（卡片渲染失败时）同步带上平台排名。

### ⚙️ 变更
- `rank_snapshot` / `progress_snapshot` 新增 `rating_rank INTEGER` 列，排行行落盘时
  一并写入平台内排名；旧库用 `PRAGMA table_info` 判定缺列后 `ALTER TABLE` 幂等迁移，
  失败只记 warning、不阻塞启动。
- 卡片结构变化，`CARD_FORMAT_VERSION` 17 → 18，旧缓存图自动失效。

### ⚠️ 升级注意
- 首次启动会对既有 SQLite 库执行一次 `ALTER TABLE ... ADD COLUMN rating_rank`（幂等，
  不影响旧数据）；升级后排行卡会重新渲染一次，缓存短暂 miss 属正常。

---

## [1.14.1] - 2026-09-18

> 📐 平台排名显示带上百分位：`平台排名 #48 · Top 0.04%`。

### ✨ 新增
- **排名百分位**：AtCoder 直接采用平台给出的 `Top 0.04%`；
  Codeforces 用「名次 / 全站已评级人数」现算（37992 人 → `#1200 · Top 3.2%`）；
  牛客与洛谷没有总人数，只显示名次。
- 新增 `_platform_rank_text()` 统一排名文案，资料卡、群排行与文字兜底共用同一套格式。

### 🐛 修复
- 牛客个人页对高排名会**截断显示成 `9999+`**，此前会被解析成名次 9999；
  现在忽略带 `+` 的截断值，只采用 `rating-basic` 接口的精确名次（实测 14410）。

---

## [1.14.0] - 2026-09-18

> 📈 资料卡的「平台排名」补齐到四个平台（此前只有牛客/洛谷有）。

### ✨ 新增
- **Codeforces 全站排名**：按 `user.ratedList`（已评级用户，按 rating 降序）算出该 rating 的名次，
  资料卡显示「平台排名 #N」。实测 3.8 万人 / 13.5MB / 约 20 秒，
  因此**只落盘排序后的 rating 数组**（169KB）并用二分定位名次，24 小时有效；
  命中缓存后名次计算为 0 成本。
- **AtCoder 全站排名**：解析用户页 `Rank` 行（形如 `48th (Top 0.04%)`），
  资料卡显示「平台排名 #N」。
- 牛客（个人页 `Rating排名`）与洛谷（用户 JSON `ranking`）此前已支持，本次一并纳入回归测试。

### ⚙️ 变更
- `rating_rank` 现在四平台齐备；群排行/资料卡的既有展示逻辑无需改动即可显示。
- 新增 CF 全站 rating 榜缓存文件 `codeforces_rated_ratings.json`（与题库索引同目录）。

### ⚠️ 升级注意
- 升级后**首次**拉取某个 Codeforces 用户的详细资料会多约 20 秒（拉全站榜单），
  之后 24 小时内所有用户都走缓存、无额外耗时。

---

## [1.13.3] - 2026-09-18

> 🐛 修复赛后赛果推送的三个线上问题：**跨群串台**、卡片 emoji 变方块、列表没有表头。

### 🐛 修复
- **跨群串台（严重）**：赛果结果缓存只按「平台 + 比赛」作键，**没把成员集合算进去**，
  于是第一个群算出的赛果被其余所有群复用——线上真实故障：5 个群都推了主群那一个人的成绩，
  而其他群根本没人参加。现在缓存键加入**成员指纹**，每个群只播报本群成员的成绩。
- **赛果卡 emoji 变成方块 X**：服务器上没有 Chromium，卡片走 Pillow 兜底渲染，
  用的 CJK 字体没有 emoji 字形。现在卡片文本（标题/副标题/昵称/备注）统一清洗 emoji；
  QQ 消息里的 emoji 由客户端渲染，**不受影响**。
- **赛果卡列表没有表头、列不对齐**：HTML 路径本来有「名次/成员/通过/参赛人数」表头，
  Pillow 路径漏了，而且整行是一段拼接文本。现在 Pillow 路径补上表头，并把行改成
  **四列对齐绘制**（名次左对齐、成员列截断、通过/参赛人数右对齐）。

### ⚙️ 变更
- 赛果采集拆出「原始数据缓存」（CF 榜单 / AtCoder results）：与「哪些群」无关的数据跨群共享，
  避免每个群重复拉取 248KB；结果缓存则按群隔离。
- 新增诊断日志：巡检发现候选、以及**每次跳过推送的原因**（本群绑定人数不足 / 采集失败 /
  本群成员均未参赛）都会记录，便于线上排查「为什么没播报」。

---

## [1.13.2] - 2026-09-18

> 🐛 修复「@机器人 发 `我的战绩` 没反应/回错话」——被误判成"查询被 @ 那个人的资料卡"。

### 🐛 修复
- **`我的战绩` / `我的账号` 在 QQ 群里失效**：QQ 群里几乎每条指令都会 @ 机器人，
  而这两个词同时出现在"查他人资料卡"的说法集合里，于是被路由到"查询被 @ 的用户"分支，
  被查的其实是机器人自己 → 回一句"该成员还没有绑定竞赛平台账号"。
  `我的cf` / `我的洛谷` / `我的牛客` 不在该集合里，所以一直是好的（线上日志可证）。
  现在把它们**从"查他人"集合移出**，并加一层兜底：无论 @ 了谁，这两个词都只查自己。
- **加固"@的是机器人自己"的识别**：原先只靠 `event.get_self_id()`，而 QQ 官方适配器
  有时返回占位符（`qq_official`），导致机器人的 @ 被当成普通成员。现在会同时参考
  `message_obj.self_id` / `raw_message.self_id`，并识别消息链里 `is_you` 标记的 At 组件。

### ⚙️ 变更
- `@某人 战绩` / `@某人 查询战绩` / `@某人 资料卡` 等"查他人"用法**行为不变**（有回归测试）。

---

## [1.13.1] - 2026-09-18

> 📖 提供 `intro.md`：安装「菜单导航」插件后，发送 `插件介绍` 即可看到本插件的完整功能清单。

### ✨ 新增
- 根目录新增 `intro.md`，按功能分组列出全部能力，**包含菜单里看不到的主动推送**
  （每日早报、赛前 15 分钟提醒、赛后赛果、群训练周报、报名截止提醒）。
- 支持 `插件介绍 acmer` 只看本插件；菜单不受影响（菜单只收录用户发送的指令）。

### ⚙️ 变更
- 仅新增文档文件与说明，**无任何功能/行为变更**，可平滑升级。

---

## [1.13.0] - 2026-09-18

> 把比赛信息从「赛前通知」补成「赛后闭环」，并把题库数据变成每天都会用的功能：
> 新增**赛后赛果推送**、**每日一题 / 推荐补题**、**群训练周报**与**牛客报名截止提醒**。

### ✨ 新增
- **赛后赛果推送**：比赛结束后（默认 10 分钟）向"本群有成员参赛"的群推一张赛果卡，
  展示 **名次 / 通过题数 / 参赛人数**，Codeforces 另附"本场未通过题目"。
  - 数据源：Codeforces 官方 `contest.standings`、AtCoder 官方 `results/json`、牛客个人参赛记录；
    洛谷不公开比赛榜单，降级为"参赛名单"并在卡片上写明口径。
  - **卡片不含 Rating 变化**：CF 要等系统重测、牛客固定次日 00:00 才更新评分，等不起；
    名次类信息赛后即可获得（实测牛客名次与评分字段完全解耦）。
  - 每群每场只推一次（幂等键 `settle_<群ID>_<平台>_<比赛ID>`），发送成功才记录；
    失败下一周期重试，超过 2 小时不再补推；无人参赛则静默跳过。
- **每日一题 / 推荐补题**：
  - 早报末尾附"今日一题"，`每日一题` 指令可随时查看，**两者同源同一题**；
    按本群该平台 Rating 中位数选难度、按星期轮换知识点（35 个规范标签），
    用 `sha1(群ID:日期)` 取模——**确定性抽题、可复现**，不依赖随机数。
  - **展示只报题目名 + 链接**：难度与知识点仅用于选题，不写进消息（牛客索引为此新增题目名字段）。
  - 单平台详细资料卡之后追加 3 道"推荐补题"（未通过 + 贴合当前难度 + 优先薄弱知识点）。
  - 默认题目源为牛客（复用本地题库索引，**零新增抓取**），可切换到 Codeforces / AtCoder / 洛谷
    （首次使用后台构建题目索引并落盘，7 天有效）。
- **群训练周报**：默认每周一 20:00 推送「本周进步榜 / 退步榜」两张现有卡片，
  外加一段文字统计（参与人数、人均活跃天数与提交数、进步最多 / 退步最多）。
  活跃数据只取最近 7 天（CF `user.status`、AtCoder kenkoooo 提交，1~2 请求/成员，缓存 6 小时）；
  牛客 / 洛谷没有等价的轻量接口，不统计活跃并在文案中如实标注。
- **牛客报名截止提醒**：报名截止前 24 小时与 2 小时各提醒一次，
  含比赛名、截止时间（北京时间）、剩余时长与链接。
- **新增 WebUI 配置**：赛后赛果推送 / 推送延迟 / 最少参赛人数 / 附带未通过题目、
  早报今日一题与题目平台、群训练周报开关与时间；群列表新增逐群「赛后赛果」开关。

### ⚙️ 变更
- `CARD_FORMAT_VERSION 16 → 17`：新增赛果卡类型，旧缓存图自动失效。
- `ANALYSIS_CACHE_VERSION 4 → 5`：分析结果新增 `solved_problem_ids`（牛客 / CF / AtCoder，各上限 2000），
  供"排除已通过题"与推荐补题使用；旧分析缓存自动失效。
- `CACHE_VERSION 1 → 2`：`Contest` 新增 `signup_end_time` 字段，旧赛程缓存整体失效并重新抓取。
- 牛客题库索引新增**题目名**字段（`NOWCODER_PROBLEM_INDEX_VERSION 1 → 2`）：
  旧索引会被忽略并在下次巡检时自动重建（约 289 次请求 / 2.5MB，实测约 36 秒）。
- Codeforces 榜单请求固定**只带 `contestId`**（官方要求匿名 GET 且不带其它参数），
  该请求超时放宽到 30 秒（实测单场 248KB / 约 10 秒）。

### ⚠️ 升级注意
- **会多两类推送**：赛后赛果（赛后 10 分钟）与群训练周报（默认周一 20:00）。
  不需要就在 WebUI 关闭「赛后赛果推送」/「群训练周报」，或在群列表里逐群关闭。
- **升级后会重建一次牛客题库索引**（新增题目名字段，约 289 次请求 / 36 秒，后台完成），
  期间"今日一题"暂不追加，建好后自动生效。
- **首次使用非牛客题库会后台建索引**（CF 约 1.1 万题、洛谷约 1.7 万题），
  期间"今日一题"暂不追加，建好后自动生效；索引落在插件 `data/` 目录，7 天有效。
- 升级后**首次刷新会重新抓取一次赛程**（缓存版本提升所致），属正常现象。
- 赛果推送要求插件在比赛结束前至少抓过一次赛程（正常运行必然满足）；
  若刚重启就遇到比赛结束，会在赛后 2 小时窗口内补推。

---

## [1.12.0] - 2026-09-18

> 牛客数据范围全面对齐其他平台：赛程修复漏收 34% 牛客赛事并改为滚动 3 个月窗口；
> 资料卡练习记录上限 2000 → 20000 条，难度/知识点改由题库索引覆盖全部通过题。

### 🐛 修复
- **牛客赛程漏收**：牛客日历接口的 `ojName` 实际取值是英文 `NowCoder`，而旧代码判断的是
  `"牛客" in ojName`（恒为假），判定退化成“比赛名含牛客”，实测 14 个月 229 场牛客赛事
  丢掉 78 场（34%，2026-03 单月高达 57%）。现改为按 `ojName`（OJ 归属）判定，
  高校校赛、新生赛同步赛、集训营、自主创建赛等不再被误过滤。
- 修复牛客资料卡“难度分布”把未评定题目误计入最低档的问题：题库接口中 `difficulty` 的
  1~5 / -1 是“未评定”哨兵值（题库列表页同题难度为空），现统一按“未标难度”处理。

### ✨ 新增
- **牛客赛程滚动窗口**：抓取范围由“当前月 + 下月”扩展为**当前月及后续 3 个月**，
  首次抓取并发取月；当前月/下月失败仍走旧缓存兜底，更远月份失败只记日志不影响结果。
- **牛客赛事范围开关**（WebUI「牛客赛事范围」）：`全部牛客赛事`（默认）/ `仅牛客系列赛`
  （等价旧口径）。抓取始终取全量，口径只在返回时过滤，切换立即生效。
- **牛客题库索引**：整库抓取牛客题库列表（14446 题，289 页，实测约 36 秒 / 索引落盘 0.72MB），
  之后难度与知识点按题目 ID 本地查表，索引 7 天自动更新、插件重启直接复用；
  实测覆盖 949/1106（旧实现为 300/1106）。
- 资料卡练习记录上限由 20 页 × 100 条提升到 **100 页 × 200 条 = 20000 条**（与 AtCoder 对齐），
  实测 3816 条提交的重度用户由“只读 52%、不给 AC 率”变为完整读取并给出 AC 率。
- 洛谷比赛列表增加翻页保险：第 1 页填满且最后一条仍未开始时继续取下一页（最多 3 页）。

### ⚙️ 变更
- 牛客练习页 `pageSize` 由 100 提升到服务端上限 **200**（同样数据量请求数减半，总流量更低）。
- 数据不足时的覆盖率文案改为三态：`完整` / `最多读取 20000 条 / 100 页` / `部分分页读取失败`，
  并在题库索引尚未构建时追加 `（题目索引构建中）`。
- Rating 历史窗口 200 → **1000 场**（历史本就在同一次请求里返回，零额外网络开销），
  牛客/洛谷由历史推导的“最高 Rating”不再可能遗漏早期峰值。
- 分析缓存版本号 3 → 4，旧分析缓存自动失效，不会残留“最多读取 20 页”的旧文案。
- 赛程抓取新增一行统计日志（原始场次 / 保留场次 / 其他 OJ 场次），便于核对口径。

### ⚠️ 升级注意
- **牛客赛事数量会明显增加**：早报、`nk比赛`、`最近比赛` 与赛前 15 分钟提醒都会覆盖
  高校校赛/新生赛同步赛等。若只想看系列赛，在 WebUI 把「牛客赛事范围」切到 `仅牛客系列赛`。
- **首次运行会后台构建题库索引**：约 289 次请求 / 2.5MB，实测约 36 秒；期间资料卡照常可用
  （难度与知识点先按单题兜底抓取，覆盖率文案会标注“题目索引构建中”），索引落在
  AstrBot 数据目录 `plugin_data/acmer_qq_group_bot/nowcoder_problem_index.json`。
- 少数只存在于比赛中的题目（定制自测题等）牛客未公开难度与知识点，仍会显示为“未标难度”，
  属平台数据限制；为避免反复白等，这类题目会被记入负缓存，不再重复补查。

---

## [1.11.0] - 2026-09-08

> 早报周榜改为两张独立图片：正文保持原样，进步榜与退步榜作为图片追加在后面。

### ⚙️ 变更
- 早报发送顺序固定为「早报正文（有比赛时才有，排版不变）→ 本群本周进步榜图片 → 本群本周退步榜图片」，
  图片内容与 `本周进步榜` / `本周退步榜` 指令完全一致（四平台分栏、每平台前 5 名）。
- 1.10.0 曾把周榜**文字**合并进早报正文，导致早报被拉长甚至可能被整段转图，本次改为独立图片。
- 当天没有比赛时不发正文，但仍会推送这两张榜单图片；两张都没有数据时才不推送。
- WebUI「测试推送」同样会追加这两张图，便于后台预览。

### 🐛 修复
- 图片渲染不可用时，自动回退为该榜单的纯文本，信息不丢失。

### ⚠️ 升级注意
- 无配置变更；若此前依赖 1.10.0 的“周榜文字合并进正文”形态，升级后会回到“正文 + 两张图”。

---

## [1.10.0] - 2026-09-08

> 每日早报附带本周进步榜与本周退步榜。

### ✨ 新增
- 早报自动追加「📈 本周进步榜」与「📉 本周退步榜」，每个平台显示前 3 名（`昵称 +变化（当前 Rating）`），
  并附完整榜单指令提示。
- 当天没有比赛时不再直接跳过早报：正文省略，但只要周榜有数据就照常推送。
- 周榜沿用早报的开关与推送时间（`morning_push_time`），不新增配置项。

### ⚙️ 变更
- 周榜与交互指令共用同一份进步榜快照，不额外抓取平台数据；单个平台失败只跳过该平台并记录日志。
- 没有完整一周快照数据时不产生空白区块。

---

## [1.9.1] - 2026-09-08

> 排行同步失败的提示改为精确说明：哪个平台、多少账号、什么原因、影响范围。

### 🐛 修复
- 此前只输出 `⚠️ 部分账号同步失败，排行可能不完整：洛谷、AtCoder`，看不出失败数量与原因；
  现在会写明平台、失败账号数、一条原因（超长截断到 40 字）与影响（这些账号本次不计入排行）。

### ⚙️ 变更
- 经复测确认，此前观察到的洛谷 / AtCoder 全量失败属于平台侧瞬时失败（限流 / 超时），与查询并发度无关。

---

## [1.9.0] - 2026-09-08

> 新增「本周退步榜」，与进步榜共用同一份数据。

### ✨ 新增
- `本周退步榜`（别名 `本周掉分榜`、`退步榜`）：展示各平台近 7 日 Rating **下降最多**的成员，按下降幅度从大到小排序。
- 排版与进步榜一致：四平台分栏、每平台前 5 名、`近7日变化` 为主指标、右侧附 `当前 Rating`。
- 无成员下降时给出明确文案，而不是复用进步榜的空态提示。

### ⚙️ 变更
- 与进步榜共用 progress 快照（同样取 `近 7 日变化`，只在筛选与排序上不同），无新增网络请求与存储表，
  快照新鲜窗口同为 2 小时。
- 只统计拥有完整一周快照的成员，缺少历史基线的成员不计入。

---

## [1.8.2] - 2026-09-08

> 加大打卡热力图的内部间距，标题、方格与图例不再看起来挤在一起。

### 🐛 修复
- 实测标题与方格间距仅 4～5px、方格与图例仅 8px，小尺寸查看时像重叠；现调整为 11px / 12px，
  区块上下留白由 4px 增至 6px。
- 间距改为共享常量，绘制与高度估算共用同一套公式，修正了两处估算与实际绘制相差 4px 与 12px 的问题。
- HTML 渲染路径同步加大留白（标题下边距与图例上边距 8→12px）。

---

## [1.8.1] - 2026-09-08

> 热力图空态不再整块消失，明确区分“平台不支持”与“近一年没有提交”。

### 🐛 修复
- 近 12 个月没有提交时，热力图区块此前完全不渲染，容易被误认为功能没生效；现在显示一行说明，例如
  `近 12 个月暂无提交记录（历史共 82 条提交，均在一年前）`。
- 严格区分两种情况：平台不支持该区块（牛客 / 洛谷）时不显示；平台支持但近一年无提交时显示上述说明。
- 对齐热力图高度估算与实际绘制公式，避免卡片高度预算偏差。

### ⚙️ 变更
- 明确指令口径：`我的cf` 与 `@查询` 总是抓分析（含热力图）；`查询cf <账号>` 默认只抓基础资料，需加「详细」才有热力图与难度分布。

---

## [1.8.0] - 2026-09-08

> 战绩卡新增「近 12 个月打卡」热力图（Codeforces / AtCoder）。

### ✨ 新增
- 7 行（周一～周日）× 最多 53 列的方格矩阵，按当天提交条数分 5 档着色，并显示
  「一年活跃 N 天 · 当前连续 X 天 · 最长 Y 天」，位置在「难度分布」与「Rating 趋势」之间。
- 双平台卡宽度只有一半时自动降级为 26 周（近 6 个月）紧凑版，极窄时退化为纯文字摘要。
- 复用已有的提交扫描结果，零新增网络请求；随分析缓存 12 小时。

### ⚙️ 变更
- 提交扫描翻页只以「读完公开记录 / 到达上限」为准，不再为热力图提前收手（此前会让重度账号的提交数与通过题数被截断）。
- 分析缓存引入版本号，口径变化后旧持久化缓存自动失效，无需等待 TTL。

---

## [1.7.1] - 2026-09-08

> 修复“扫描上限提高了，分析摘要却还显示旧数据”。

### 🐛 修复
- 个人资料卡分析结果此前持久化缓存 12 小时（重启也不清），提高扫描上限后仍显示旧的
  `最近 10000 条公开提交（最多读取 10000 条）`。
- 现为分析类缓存引入版本号：加载时只接受当前版本，旧版本缓存直接跳过，下次查询即按新上限重新抓取，
  无需等待 TTL 或手动清库。

---

## [1.7.0] - 2026-09-08

> 提高提交扫描上限，重度选手的提交数与通过题数不再被低估。

### ⚙️ 变更
- Codeforces 扫描上限 10000 → 50000，AtCoder 10000 → 20000。
- Codeforces 改为翻页扫描（每页 10000 条），直到某页不满或到达上限；普通账号仍然只发 1 次请求，
  开销与旧实现相同。AtCoder 分页增加页间间隔，避免把公共接口打得太急。

### 🐛 修复
- 修复覆盖率文案自相矛盾（出现「最近 21459 条公开提交（最多读取 10000 条）」这类提示），
  现在按「最后一页是否满页」判断，读完显示「已读完当前公开记录」。

---

## [1.6.2] - 2026-09-08

> 修正段位显示：`LGM` 缩写与「当前段位」标签。

### 🐛 修复
- `legendary grandmaster` 的缩写由 `LG` 修正为社区标准的 `LGM`。
- 头部不再把段位直接跟在 `Rating` 标签后面，统一显示为 `当前段位：LGM`，避免被理解为 Rating 的取值。
- HTML 截图路径与 Pillow 路径共用同一套段位缩写，两条渲染路径显示一致。

---

## [1.6.1] - 2026-09-08

> 修复战绩卡中长段位文本越界压住相邻单元格。

### 🐛 修复
- Pillow 卡片路径此前所有文本都按固定列宽绘制，`最高段位：International Grandmaster`
  这类长文本会越界；现按「段位缩写 + 自适应列数 + 省略号兜底」三层处理。
- 统计网格先测量最宽单元格再决定 4/3/2 列；仍超宽的单元格用 `…` 截断，保证永不越界。
- 修复头部「当前段位」在双平台卡（可用宽度约 123px）下越出卡片的问题。

---

## [1.6.0] - 2026-09-08

> 新增未绑定用户战绩查询。

### ✨ 新增
- `查询cf <用户名>`、`查询atcoder <用户名>`、`查询牛客 <数字UID>`、`查询洛谷 <数字UID>`，
  输出与 `@查询战绩` 一致的资料卡（渲染失败自动回退纯文本）。
- 兼容 `查询 cf <账号>`（带空格）、`查cf` 简写与主页链接；结尾加「详细」才会抓取难度分布与题目级分析。
- 群聊结果附带绑定引导。
- 防滥用：每用户 30 秒 1 次 + 每群每分钟 10 次，超限提示剩余等待时间。

### ⚙️ 变更
- 未绑定查询不写入 Rating 快照、不参与群排行与进步榜，卡片不展示「本群排名」并标注「（未绑定）」。
- 平台能力限制：只有 Codeforces / AtCoder 支持按用户名查询；牛客、洛谷公开接口只能按数字 UID 或主页链接查询。

---

## [1.5.1] - 2026-09-08

> 修复菜单退化为文字版，并大幅提速本周进步榜。

### 🐛 修复
- 1.3.2 为保留短文本换行调整了配置读取顺序，导致首次请求时渲染阈值仍是默认值，32 行的菜单被判为“不需要转图”而发文字；
  现在在热路径上同步应用缓存的阈值配置，并在插件启动时预读一次。

### ⚙️ 变更
- 进步榜改用轻量资料（Codeforces 走批量 `user.info`），差值优先由本地 Rating 历史计算，
  仅对缺少基线的成员限量回退抓取详细资料（每轮 8 人并轮转）。此前首次计算需逐个成员拉历史，实测耗时 93 秒。

---

## [1.5.0] - 2026-09-08

> 本周进步榜纳入快照，命中即 O(1) 返回。

### ⚙️ 变更
- 进步榜新增独立快照表，与群排行共用同一套 stale-while-revalidate 读模型：命中快照直接返回，
  过期时先返回旧数据并后台刷新。
- 进步榜新鲜窗口为 2 小时（排行仍为 60 分钟），绑定 / 解绑 / 改名会把两个快照一起标脏。
- 后台刷新分别扫描两种快照，进步榜只刷新“曾被查询过”的群，不会为全量群做昂贵的重算。

---

## [1.4.0] - 2026-09-08

> 比赛数据后台预热，并收紧渲染与缓存的稳定性。

### ⚙️ 变更
- 定时任务在比赛缓存到期前 90 秒提前刷新（每轮最多 1 个平台），用户请求不再撞上“缓存过期后同步抓取”的卡顿；
  预热失败按平台指数退避（5 分钟起，最长 30 分钟）。
- 修复强制刷新仍命中未过期缓存的问题（`update` 与预热行为符合预期）。
- 账号摘要 / 详细资料缓存由 10 分钟放宽到 30 分钟，减少重复抓取；`刷新我的战绩` 等强制路径仍即时。
- 渲染器与字体探测结果增加进程级缓存；渲染失败增加 300 秒冷却；渲染产物增加 PNG 魔数校验，
  损坏图片不再被当作成功发送。
- `output_cache/`、`account_cards/` 按总容量 500MB 做 LRU 清理，避免磁盘无限增长。

---

## [1.3.2] - 2026-09-08

### 🐛 修复
- 修复短文本被动回复丢失换行的问题（读取配置的过程会把纯文本里的换行压平）；长文本转图逻辑不变。

## [1.3.1] - 2026-09-07

### ⚙️ 变更
- 纯文本分片保留前导缩进，并优先在行尾 / 空格边界断片，避免丢缩进或硬切长链接。
- 超过单条安全长度但配置未要求转图的长文本改为按安全长度分片发送。
- 主动推送的文字分片单片失败时继续尝试剩余分片，并记录精确失败位置。

## [1.3.0] - 2026-09-07

> 账号数据迁入本地 SQLite，群排行改为物化快照。

### ⚙️ 变更
- 账号、群成员、待确认绑定与 Rating 快照从 AstrBot KV 迁入本地 SQLite（WAL）：
  首次启动自动迁移并生成 JSON 备份，支持 KV 双写与回退。
- 资料缓存与账号级负缓存持久化到 SQLite，重启后不必全量冷启动重爬；按周期批量落盘并清理过期行。
- 群排行改为物化快照 + 后台刷新（默认 60 分钟），普通查询优先命中本地快照，不再全群同步抓取。
- 全局设置与群配置增加内存缓存；定时任务每个周期只读取 / 抓取一次比赛数据。
- Codeforces 批量 `user.info` 失败时只回退失败批次，并增加账号级负缓存，避免坏账号反复拖慢排行。
- 增加渲染并发上限、账号抓取缓存上限与关键路径耗时日志。

### ⚠️ 升级注意
- 默认对用户可见的指令与文案不变。相关环境变量：`ACMER_STORE_BACKEND`（`sqlite`/`kv`）、
  `ACMER_STORE_DIR`、`ACMER_DUAL_WRITE_KV`；如需回退旧 KV 模式设置 `ACMER_STORE_BACKEND=kv`。

---

## 1.2 系列（账号绑定 · 战绩卡 · 群排行）

> 1.2.0 引入四平台账号绑定、个人战绩卡与群排行；1.2.1～1.2.28 围绕绑定校验、
> 卡片渲染与中文/Emoji 字体反复打磨。

### 1.2.0 – 1.2.9（战绩卡与排行的基础）

**1.2.0 — 四平台账号绑定、战绩卡与群排行**
- ✨ 新增 Codeforces、牛客、洛谷、AtCoder 账号绑定：一次性验证码，追加到 CF 姓氏、牛客个性签名、洛谷个人介绍、AtCoder Affiliation（所属）；验证码只保存哈希，10 分钟有效。
- ✨ 新增 `我的账号`、`我的战绩`、`我的cf`、`我的牛客`、`我的洛谷`、`我的atcoder`，资料以 PNG 卡片展示。
- ✨ 绑定成功后自动加入发起绑定群的排行；新增 `群排行`、各平台排行、`本周进步榜`、`退出群排行`。
- ⚙️ 新增账号资料缓存、Codeforces 请求间隔控制、公开数据失败兜底与卡片缓存。
- ⚠️ 洛谷个人页无法读取或个人介绍不可见时，明确提示「暂时无法绑定」。

**1.2.1 — 绑定校验统一**
- 🐛 Codeforces 的 `Last name/姓氏` 支持像其他平台一样追加验证码，不再因原本已有姓氏而验证失败。
- ⚙️ 四个平台的绑定提示与校验逻辑统一：字段中包含完整验证码即可通过。

**1.2.2 — 个人资料卡布局**
- ⚙️ 单平台使用整行紧凑卡片，多平台卡片按平台数量与内容自适应高度，减少底部空白。
- 🐛 修复 Chromium 截图临时文件后缀不正确时无法生成 PNG 的问题。

**1.2.3 — 大群排行**
- ⚙️ 平台榜单固定每页最多 30 人并支持页码（如 `群cf排行 2`、`群cf排行第2页`）；`群排行` 与 `本周进步榜` 保留各平台前 5 名总览。
- ⚙️ 增加排行结果 5 分钟缓存、缓存上限、分批读取与并发限制；快照改为批量读写，减少大群排行时的数据库操作。

**1.2.4 — 排行卡高度**
- 🐛 卡片高度改为按行数、长昵称 / 账号换行与翻页提示动态计算，不再裁切底部排行行。

**1.2.5 — 总览卡叠层**
- 🐛 修复群排行总览在 Pillow 回退渲染时因分区高度不同导致的叠层，第二行按上一行分区的最大高度定位。

**1.2.6 — 群内排名与头像**
- ✨ 个人战绩卡在群聊中显示当前用户的本群排行；资料卡增加平台头像缩略图（正方形容器裁剪填充，失败时用平台色占位）。
- ⚙️ 私聊资料卡不显示群排行。

**1.2.7 — 头像与视觉主题**
- 🐛 修正 Codeforces 头像读取（改用 API 的 `avatar`，不再误用横向 `titlePhoto`）；增加 AtCoder 头像与洛谷头像兜底地址。
- ⚙️ 卡片、群排行、比赛长消息与后台配置页统一为樱粉 / 珍珠白 / 冰蓝的花瓣水晶主题，Pillow 回退渲染同步。

**1.2.8 — 绑定用法提示**
- 🐛 缺少账号参数时给出用法提示，明确所需账号类型与验证码对应的公开资料字段。

**1.2.9 — 绑定与渲染的健壮性**
- 🐛 `确认绑定<平台> <非法账号>` 不再被静默接受；跨群完成确认时优先保留发起绑定时记录的群。
- 🐛 卡片渲染异常统一回退为文字结果；快照写入、昵称更新等可选步骤异常时不再误报账号查询 / 绑定失败。

### 1.2.10 – 1.2.18（洛谷绑定 · 字体 · 排行表头 · 题目级分析）

**1.2.10** — 🐛 洛谷绑定验证码改为读取 `api.luogu.me/user/query/{uid}` 的 `introduction`；其余资料仍用 `luogu.com.cn`。
**1.2.11** — 🐛 洛谷资料与验证码改为优先读取 `www.luogu.com/user/{uid}` 的 `introduction`，`.com` 不可用时兼容 `.com.cn`；个人介绍为空的账号明确反馈「暂时无法绑定」。
**1.2.12** — ✨ Codeforces 资料卡增加通过题难度分布（按不同题目去重，`≤999` 至 `3200+` 及未标分）；⚙️ 排行增加「名次、成员/账号、当前指标、近7日变化」表头。
**1.2.13** — ⚙️ 资料卡新增最近 8 场 Rating 趋势折线图，Codeforces 新增通过题难度条形图；难度区间细分为 `1000–1199`～`3000–3199` 与 `3200+`。
**1.2.14** — 🐛 区分「洛谷个人介绍字段存在但为空」与「字段缺失 / 读取失败」；空字段时给出填写提示，确认阶段保留待确认记录可直接重试。
**1.2.15** — 🐛 洛谷个人介绍为空时仍正常发放绑定验证码，追加后即可确认绑定。
**1.2.16** — 🐛 修复中文字体选择：对 Noto Sans CJK TTC 显式使用简体中文字体面，HTML/Chromium、Firefox 与 Pillow 统一字体链。
**1.2.17** — ⚙️ 放宽菜单、比赛长消息与 Pillow 渲染的字间距、行距与条目留白，缓解中文内容过密。
**1.2.18** — ✨ 牛客 / AtCoder 资料卡增加难度分布、知识点或题目系列、提交通过率、常用语言与活跃度；洛谷增加原生难度分组、题库类型、活动日历与 Elo 历史；⚙️ 题目级分析只在详细资料命令触发（分析缓存 12 小时、题目资源缓存 24 小时）。

### 1.2.19 – 1.2.28（排行指标 · 配色 · @查询 · Emoji）

**1.2.19** — ⚙️ 排行指标表头按平台显示「当前 Rating」「当前 Elo」「当前平台排名」，不再使用笼统的「当前指标」。
**1.2.20** — ⚙️ 放大排行表头与数值、提高对比度，HTML 与 Pillow 同步调整列宽 / 行距 / 高度估算。
**1.2.21** — 🐛 修复字体放大后昵称、账号与表头互相贴合；长文本限制列宽、长表头分行。
**1.2.22** — 🐛 重做排行表头与数值列布局，「近7日变化」保持单行并留出安全边距。
**1.2.23** — 🐛 进一步收紧列边距，长昵称不再挤占指标与变化列。
**1.2.24** — ⚙️ 调整资料卡与排行卡配色：浅色卡片用更深的正文色，深色背景用更亮的标题 / 页脚色，正负变化统一为深绿 / 深红。
**1.2.25** — ✨ 群聊支持 `@某人`、`@某人 战绩`、`@某人 查询战绩` 查询成员战绩卡，只读且不改变对方排行状态。
**1.2.26** — ⚙️ 取消「单独 @某人 即触发战绩卡」，必须追加「战绩」「查询战绩」等明确后缀，降低误触发。
**1.2.27** — 🐛 修复本周进步榜洛谷平台排名列标题过长被省略，改用更紧凑的「排名」表头。
**1.2.28** — 🐛 菜单与长消息中的 Emoji 不再交给中文字体绘制，插件内置 Noto Color Emoji 字体，服务器缺少 Emoji 字形时不再出现方框。

---

## 1.1 系列（赛程查询 · 线下赛 · 主动推送）

> 1.1.0～1.1.27 完成赛程查询体系：全部未开始比赛与最近一场、线下赛、
> 自适应长消息转图、主动推送激活与配置页。

### 1.1.0 – 1.1.9（赛程查询与菜单）

**1.1.0** — ⚙️ 平台查询改为返回**全部未开始比赛**列表（最多 30 场，超出提示总数）；✨ 新增各平台「最近一场」查询。
**1.1.1** — 🐛 牛客日历对 AtCoder 收录不全（实测 9 月起为空），改为直接解析 AtCoder 官网赛程表。
**1.1.2** — ⚙️ 牛客指令 `/nc` 改为 `/nk`，`/最近nc` 改为 `/最近nk`（中文别名不变）。
**1.1.3** — ⚙️ 明确标注 @全体成员在 QQ 官方群聊不生效（即使机器人群管理员也无法真正 @ 到人）。
**1.1.4** — ⚙️ 指令分发改为手动前缀匹配，不再依赖 `@filter.command`，`astrbot_version` 下限放宽到 `>=4.13.0`。
**1.1.5** — 🐛 `metadata.yaml` 补充 `repo` 地址，修复 WebUI「更新插件」报错，可在插件管理页一键更新。
**1.1.6** — ⚙️ 补充「安装后无响应」的排查指引：检查「可用插件」（`plugin_set`）是否包含本插件。
**1.1.7** — ⚙️ 指令改为全匹配，避免「牛客xxxx」等聊天内容误触发。
**1.1.8** — ✨ 新增 `menu.md`，供「菜单导航」插件聚合本插件菜单。
**1.1.9** — ⚙️ 菜单末尾附带开源仓库链接。

### 1.1.10 – 1.1.17（推送逻辑与菜单优化）

**1.1.10** — ⚙️ 菜单排版优化：分区图标、命令分隔符与分隔线。
**1.1.11** — ⚙️ 菜单补充「显示本菜单」条目与全匹配 / `/` 前缀提示，「最近一场」统一为「最近一场比赛」。
**1.1.12** — ⚙️ 移除菜单里的聚合菜单导航条目，聚合菜单由「菜单导航」插件负责。
**1.1.13** — 🐛 测试推送在「今日无比赛」时不再失败，自动改发最近一场比赛作为测试内容。
**1.1.14** — ⚙️ `/acm` 快捷菜单改为 `/acm菜单`（`比赛帮助` 别名保留）。
**1.1.15** — 🐛 赛前提醒去重键改为按群隔离，同一场比赛每个启用群都会各收到一次。
**1.1.16** — 🐛 `repo` 显式指定 `tree/master`，WebUI 更新不再依赖 `api.github.com` 查询默认分支。
**1.1.17** — 🐛 兼容 QQ 官方指令面板自动添加的 `/` 前缀。

### 1.1.18 – 1.1.27（线下赛 · 长消息转图 · 主动推送）

**1.1.18** — ✨ 新增 `线下赛`（`线下比赛`、`XCPC线下赛`）：从 XCPC Link 提取 ICPC/CCPC 等线下赛事并过滤网络赛，支持 `xcpc.link` 与备用 `xcpc.ink`，结果标注实际数据源。
**1.1.19** / **1.1.20** / **1.1.21** — 🐛 兼容 AstrBot 更新过程中插件文件暂时不同步的情况，并改用相对包导入隔离残留模块缓存；检测到旧版抓取器时提示完整重装，而不是加载失败。
**1.1.22** — 🐛 查询指令统一忽略英文大小写（`XCPC线下赛`、`xcpc线下赛`、带 `/` 前缀均可触发），中文指令仍全匹配。
**1.1.24** — ⚙️ 线下赛增加 30 分钟内存缓存与 `data/contest_cache.json` 持久化缓存；同一平台并发请求合并，网络失败时优先用旧缓存兜底。
**1.1.25** — ⚙️ 菜单、比赛列表、线下赛列表、刷新结果与主动推送统一自适应输出：短内容发文字，长内容转 PNG 图片，按内容哈希缓存；转图在后台线程执行，支持 Chromium/Chrome、Firefox、`wkhtmltoimage`，没有浏览器时用 Pillow，全部不可用时拆分为文字发送。
**1.1.26** — ✨ WebUI「推送设置」新增文字转图片阈值（最大字符数与最大行数），保存立即生效；后台提交值增加范围校验。
**1.1.27** — ✨ 新增 `最近比赛`（`近期比赛`）汇总各平台近期赛程并统一排序；WebUI 新增「最近比赛查询范围」，默认未来 7 天，可设 1～30 天。

---

## 1.0 系列（比赛查询与定时推送）

**1.0.0** — ✨ 首版：牛客 / Codeforces / AtCoder / 洛谷比赛查询，每日早报、赛前 15 分钟提醒，WebUI 配置页与管理员校验。
**1.0.1** — ✨ 新增功能菜单 `acmer群管理插件菜单`，群聊直接发送即可查看全部指令与所需权限。
**1.0.2** — 📄 新增 `docs/CONFIG.md` 配置文档：配置项一览、管理员权限、推送行为、数据源缓存与常见问题。
**1.0.3** — 🐛 修复 AstrBot 加载插件时子包导入失败（`No module named 'src'`）。
**1.0.4** — 🐛 修复群配置解析失败导致定时任务读不到任何群、无法推送；早报包含「今天已开始但仍在进行」的比赛。
**1.0.5** — ✨ 主动推送的平台实例 ID 改为运行时自动获取，WebUI 显示检测到的 ID 与推送前提；🐛 发送失败不再标记「已发送」，下个周期自动重试。
**1.0.6** — ✨ WebUI 群配置新增「测试推送」按钮；🐛 会话未就绪时提示「请先让群内发一条消息」，不再误报「已发送」。
**1.0.7** — ✨ 新增 `acmer激活`（`/激活`）：重启后群内发送一次即可激活本群主动推送，机器人回复时会一并缓存消息 ID。
**1.0.8** — ⚙️ 激活状态改为持久保存；重启后只要群内出现任意一条消息就会自动重新激活。
**1.0.9** — ⚙️ 激活成功后重复发送将静默忽略，不再回复确认消息。
